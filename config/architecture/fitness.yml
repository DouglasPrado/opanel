# Declarative metadata the architecture fitness functions consume.
#
# The rules live in code (lib/gates/fitness_functions.rb); the *facts* they check
# against live here, because they change as the domain grows and a rule should
# not have to be edited to learn about a new column or a new mutation.
#
# Adding to any list below widens what is enforced. Removing from one narrows it,
# and that is a gate change: it needs a Story and the Post-commit Gate flags it
# (Annex I §21.2).

# AF-03 — columns that hold the user's approved intent. A reconciler converges
# Actual State toward these; it never writes them. Platform Wins is the drift
# policy, and adopting runtime state is an explicit admin operation.
#
user_intent_columns:
  # M01-11: Environment fields representing user intent.
  - table: environments
    columns: [name, slug, type, auto_promote_secrets]
  # M01-12: Service fields representing user intent.
  - table: services
    columns: [name, slug, image_ref, replicas, ports, health_check, cpu_reservation,
              cpu_limit, memory_reservation, memory_limit, constraints]

# AF-07 — mutations that must reach a server-side authorization path. The check
# has two halves: the Policy exists with the declared action, **and** the Command
# actually invokes it. A Policy nobody calls is a file.
#
# Empty until M01-04, because there was no Policy layer to reach. A mutation is
# added here when it becomes tenant-scoped and role-dependent; a mutation that
# acts only on the caller's own rows (RevokeSession) is authorized by scoping the
# query to the owner, which is a different control and is asserted by its own
# cross-account negative test.
critical_mutations:
  - command: SuspendTeamMember
    policy: TeamPolicy
    action: manage_members?
  # M01-08. Turning a machine into the first node of a cluster is the most
  # privileged thing this Milestone can do: it needs INSTANCE_ADMIN on top of the
  # Team role, and the Command is the only path to `docker swarm init`. Declaring
  # it here makes AF-07 require that `BootstrapCluster` actually reaches
  # `ClusterPolicy#bootstrap?` rather than merely mentioning it.
  - command: BootstrapCluster
    policy: ClusterPolicy
    action: bootstrap?
  # M01-11. Creating an Environment is a tenant-scoped, role-dependent mutation
  # (ADMIN and DEVELOPER only). AF-07 requires it to reach the Policy.
  - command: CreateEnvironment
    policy: EnvironmentPolicy
    action: create?
  # M01-11. Updating an Environment (name, slug in M01) is also tenant-scoped
  # and role-dependent.
  - command: UpdateEnvironment
    policy: EnvironmentPolicy
    action: update?
  # M01-12. Creating a Service is a tenant-scoped, role-dependent mutation
  # (ADMIN and DEVELOPER only). AF-07 requires it to reach the Policy.
  - command: CreateService
    policy: ServicePolicy
    action: create?
  # M01-12. Updating Service desired state is also tenant-scoped and role-dependent.
  - command: UpdateServiceDesiredState
    policy: ServicePolicy
    action: update?

# AF-06 — field names that carry secret material. A serializer, a log formatter or
# an audit payload builder that emits one of these in plaintext is a finding.
sensitive_field_names:
  - secret_value
  - plaintext
  - private_key
  - recovery_key
  - master_key
  - password
  - passphrase
  - api_key
  - access_token
  - refresh_token
  - client_secret
  # M01-01. The raw session token exists only in the response cookie; the column
  # holds its SHA-256. Named consistently so this entry catches it wherever it is
  # written.
  - session_token

# AF-02 — the single boundary allowed to reach the Docker Engine API, plus the
# test harness that drives the disposable lab.
#
# **Adding a path here is a Tier-0 security change** (Annex C §8). It needs an ADR,
# not a Story.
docker_boundaries:
  - app/executors/
  - spec/support/swarm_lab
  - bin/swarm-lab
  - lib/gates/swarm_lab.rb

# Files that contain the patterns *because they are the checkers*. AF-02 would
# otherwise report the rule for stating the rule, and the guardrail for knowing
# what DOCKER_HOST is.
#
# Kept separate from `docker_boundaries` on purpose: that list grants the
# privilege and is a Tier-0 change. This one only says "these files talk about
# Docker, they do not talk to it". Adding to it is still a gate change.
fitness_self_referential:
  - lib/gates/fitness_functions.rb
  - lib/gates/workspace_guardrail.rb
  - config/architecture/fitness.yml
  # M01-93: the test-evidence ledger hashes the environment a run depended on,
  # and `DOCKER_HOST`/`DOCKER_CONTEXT` are part of it — a `:swarm` example that
  # passed against one daemon proves nothing about another. It reads the two
  # variables to build a digest and makes no call: same case as the guardrail
  # above, and deliberately **not** `docker_boundaries`, which is the Tier-0
  # list that grants access to the Engine.
  - lib/gates/test_evidence.rb

# Waivers. Same contract as the security waivers: reason, owner, deadline, and the
# Story that removes it. An expired waiver blocks again automatically.
#
# AF-02, AF-06 and AF-07 additionally require recorded human approval — they are
# the three that are security controls rather than layering rules, and an agent
# may not waive them (Annex I §16.3).
waivers: []
# Example:
#   - id: "af-10-2026-001"
#     function: "AF-10"
#     finding: "features/deploy/StatusPill"
#     reason: "Intentionally different from ui/badge: it animates between states."
#     owner: "douglas"
#     removal_story: "M02-07"
#     expires_at: "2026-11-01"
