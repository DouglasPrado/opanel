import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonContextMenu = createComponentSkeleton('SkeletonContextMenu', 'list');

export { SkeletonContextMenu };
