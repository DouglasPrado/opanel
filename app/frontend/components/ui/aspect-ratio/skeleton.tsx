import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonAspectRatio = createComponentSkeleton('SkeletonAspectRatio', 'media');

export { SkeletonAspectRatio };
