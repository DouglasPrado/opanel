import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonAlertDialog = createComponentSkeleton('SkeletonAlertDialog', 'overlay');

export { SkeletonAlertDialog };
