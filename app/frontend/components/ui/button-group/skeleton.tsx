import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonButtonGroup = createComponentSkeleton('SkeletonButtonGroup', 'button-group');

export { SkeletonButtonGroup };
