import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonSeparator = createComponentSkeleton('SkeletonSeparator', 'separator');

export { SkeletonSeparator };
