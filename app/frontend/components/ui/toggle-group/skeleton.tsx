import { createComponentSkeleton } from '@/components/ui/skeleton/skeleton-layout';

const SkeletonToggleGroup = createComponentSkeleton('SkeletonToggleGroup', 'control');

export { SkeletonToggleGroup };
