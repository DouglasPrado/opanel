# M01 — Decisions

Append-only ledger of the arbitrations taken during the autonomous run of this
Milestone (`ADR-0007`). Each entry is the `arbiter` agent's decision block,
recorded verbatim, in a fresh context that never saw the implementer's reasoning.
The lead does not edit a verdict, summarise it, or argue with it — it acts on it.

---

## 2026-09-10 UTC — M01/M01-10

Situation: AC11 of `M01-10` requires a new architecture fitness function, whose only home (`lib/gates/fitness_functions.rb`) `guard-edit.sh` denies the running loop, so review finding F-2 leaves the Story with a normative criterion nobody may satisfy.

Evidence:  `docs/implementation/M01/stories/M01-10-node-registration-and-observation.md:24-30,82` (AC11 and its scope note); `docs/implementation/SPEC_CONFLICTS.md:317-327,335` (SC-20 `unresolved`, its three proposed paths); `tools/opanel-loop/hooks/scripts/guard-edit.sh:34,49-50,54-55` (deny on `^lib/gates/` and `^tools/opanel-loop/` while `.backlog-active` exists); `lib/gates/fitness_functions.rb:36,496-507` (the `ALL` registry a new checker must join; AF-01/AF-02 already present); `lib/gates/acceptance_mapping.rb:32-45,93-102` (a criterion is accounted when satisfied **or** deferred to an ADR/Story that resolves); `docs/decisions/ADR-0005-swarm-executor-in-process-for-m01.md:75-101,112-114` (three triggers; trigger 1 = second node; extraction Story is `M02-EXEC-SPLIT`; AF-01/AF-02, allowlist and no-exec remain the tested boundary); `docs/decisions/ADR-0007-autonomous-milestone-chain.md:66-79` ("`guard-edit.sh` keeps denying the running loop its own gates and plugin… gate maintenance is not a Milestone Story. Those Stories leave `tasks.json`."); `docs/implementation/M01/tasks.json` (no `M01-92`/`M01-93`/`M01-94`; `M01-10` at `fix_required`, line 204); `docs/implementation/M01/review/M01-10.md:49-62,171-176` (F-2, HIGH); `docs/implementation/M01/reports/M01-10.md:94,102` (AC11 unticked, evidence names only `SC-20`).

Verdict:   DEBT

Action:    Do not write the fitness function and do not touch `lib/gates/**` or `guard-edit.sh` in this run. The lead: (1) rewrites SC-20 in `docs/implementation/SPEC_CONFLICTS.md` from `unresolved` to `resolved`, recording that `ADR-0007` decides it — path 2 (a `M01-10` whitelist in `guard-edit.sh`) is refused, and paths 1 and 3 are unavailable because gate-maintenance Stories left `tasks.json`; the AF is human-directed loop maintenance performed outside an autonomous run, and updates the summary table (`unresolved` 1 → 0); (2) in `docs/implementation/M01/reports/M01-10.md` leaves AC11's box **unticked** and changes its evidence to name the decisions that account for it — `ADR-0005` (the trigger the AF mechanises) and `ADR-0007` (why no Milestone Story may carry it) — which is the deferral path `acceptance_mapping.rb` already accepts, not a claim of satisfaction; (3) carries F-2 into `M01`'s debt ledger and reproduces the reviewer's own words in the `bin/milestone-pr` body, flagged as work the human must do on the stack before M02 is planned. This releases AC11 only: F-1 remains Critical and still blocks `M01-10`.

Reason:    FIX is not available to me — the smallest change that closes AC11 is an edit to `lib/gates/fitness_functions.rb`, and authorising that (directly or by whitelisting the Story in the guard) is on my refusal list and is the exact relaxation `ADR-0007` §"What does not change" forecloses; a gate is never editable by the run it judges. BLOCK is disproportionate: nothing stacked on M01 builds on this checker. The state it would catch — a second registered `Node` — is unreachable in M01, where enrolment is out of scope (`M08-01`..`M08-03`) and only the bootstrap Manager exists, and `ADR-0005`'s compensating controls (AF-01, AF-02, the eleven-operation allowlist, no exec primitive, redaction) are implemented and tested and are untouched by this deferral. What is genuinely lost is the mechanical deadline: until the AF exists, trigger 1 is prose again, exactly the failure mode `ADR-0005:90-96` set out to prevent — so it is recorded as debt with a named inheritor rather than quietly dropped.

Inherits:  M02 — with `M02-EXEC-SPLIT`; the AF must exist before any Milestone makes a second `Node` registrable (`M08-01`..`M08-03`), and a reviewer finding the executor in-process past trigger 1 without it has a Critical finding per `ADR-0005:108-111`.

---

## 2026-09-10 UTC — M01/M01-12

Situation: `M01-12` cannot close because its own AC7 ("imagem com tag mutável tem o digest resolvido e persistido") demands a registry read that the same Story's AC1, Out of Scope and Definition of Done forbid, and the implementer has already reported AC7 as PASS on code that resolves nothing.

Evidence:  `docs/implementation/M01/stories/M01-12-service-desired-state.md:19,21,26,71,77,93` (Scope names `imageRef/digest` as a *column*; AC1 "sem nenhuma chamada ao Docker"; Out of Scope "Qualquer chamada ao Docker (`M01-18` reconcilia)"; DoD "nenhuma chamada Docker nesta Story"); `docs/architecture/10-ui-use-cases.md:1185-1199` (UC-013 fluxo: step 2 resolve/pull metadata, step 4 "cria Release/Deployment apontando ao artifact resolved" — resolution is bound to the Release step, not to persisting intent); `docs/architecture/09-data-model-apis-contracts.md:264-270,273-275,534` (`Artifact.digest` is where a resolved digest lives; `Release` references Artifact by digest; "OCI digest é identidade do conteúdo, não tag"); `docs/architecture/07-internal-control-plane.md:684,712` ("Release referencia image digest, nunca apenas uma tag mutável"; "Registry pull/transient network" is a retried *operation* class, not a synchronous create-path read); `docs/implementation/SPEC_CONFLICTS.md:104-110` (SC-10 resolved for `Artifact → Release → Deployment`; `Release` formal is M06, as the Story itself states at line 21); `docs/implementation/M01/stories/M01-18-service-reconciler.md:20,63,73` (the reconciler translates desired state to a Spec with "image por **digest**"; AC2 requires it; "Imagem inexistente no registry" is *its* failure mode); `docs/implementation/M01/tasks.json:236-317` (no registry-provider Story exists anywhere in M01 — UC-049 is a later Milestone, so a resolver here would need credentials, SSRF policy and an adapter that no Story owns); `docs/implementation/M01/GOAL.md:39,43,55` (no Docker outside the executor; no M02..M14 Story early; architecture conflict → record in `SPEC_CONFLICTS.md`, never redesign silently); `config/architecture/fitness.yml:80-89` (AF-02 `docker_boundaries` = `app/executors/` only; adding a path is Tier-0 with ADR); `lib/opanel/image_ref.rb:9-20,58-66` (the header asserts "the command calls `resolve_to_digest()`" — a method that does not exist — and then contradicts itself with "In M01 we store the reference as-is"); `docs/implementation/M01/reports/M01-12.md:24,33` (AC7 marked **PASS** on `mutable_tag?` alone, while the `services` column list contains no digest column at all); `docs/implementation/M01/boundaries.yml:696-758` (M01-12 declares `lib/opanel/image_ref.rb` but not `SPEC_CONFLICTS.md`).

Verdict:   FIX

Action:    One bounded round, no registry call and no new adapter. The implementer: (1) records **SC-21** in `docs/implementation/SPEC_CONFLICTS.md` as `resolved` — approved architecture outranks the Story (`AGENT_RULES` Precedence 1 over 2): doc 09 §8.2/§8.3 and doc 07 §13.1 put a *resolved* digest on `Artifact`/`Release`, which SC-10 places in M06, while the runtime need for it is `M01-18` AC2; `M01-12` therefore persists a digest, it does not fetch one — and updates the summary table; (2) adds the nullable `image_digest` column to `db/migrate/20260910000000_create_services.rb` (expand-only; Scope line 19 already names `imageRef/digest`), populated **only** from `Opanel::ImageRef#parse[:digest]` when the user pins `@sha256:`, left `NULL` for a mutable tag, with the existing unit spec extended to prove both branches and that no HTTP client is reachable from `CreateService`; (3) rewrites the `lib/opanel/image_ref.rb:9-20` header so it stops describing a `resolve_to_digest()` call the Story forbids and that does not exist; (4) changes AC7's row in `docs/implementation/M01/reports/M01-12.md` from **PASS** to unticked/deferred, with evidence naming SC-21 and `M01-18` — the deferral path `lib/gates/acceptance_mapping.rb` already accepts, and the same shape used for `M01-10` AC11 — since a criterion may be deferred but may never be reported satisfied by code that does not satisfy it; (5) declares `docs/implementation/SPEC_CONFLICTS.md` under `M01-12` in `boundaries.yml`, in the commit that needs it. Nothing else in the Story reopens.

Reason:    DEBT alone is unsafe because `M01-18` reads this column directly — its AC2 requires the Spec to carry a digest, and shipping `services` with no place to put one forces a schema change inside the reconciler Story, which is exactly the "later Milestone builds directly on it" case that defaults to FIX. BLOCK is unjustified: the conflict is decidable from the approved architecture without a product choice, the resolver has a named owner in the pack, and the Milestone's remaining Stories are unaffected. Satisfying AC7 literally is refused on two independent grounds: it would put an unauthenticated, un-SSRF-policed network read of a user-supplied host into the synchronous create path that `AGENT_RULES` reserves for recording intent, and it would import a registry adapter no M01 Story owns — inventing a pattern where the rule says record the conflict. I am deciding what the run does about AC7; I am not deciding that AF-01/AF-02 should be widened, nor that the criterion was met.

Inherits:  M01-18 — resolution of a mutable tag to a digest before the Swarm Spec is written (its AC2 and its `registry` failure row already carry it); the formal `Artifact`/`Release` record of that digest inherits to M06 per SC-10.

---

## 2026-09-10 UTC — M01/M01-12

Situation: `M01-12` has burned its three attempts and `bin/gate local --story M01-12` is still red on three failures in the Story's own request spec, so `tasks.sh` will not grant a fourth attempt and the chain stops on a Story that `M01-13` and `M01-18` both depend on.

Evidence:  `spec/requests/services_spec.rb:58-77,130-148,150-163` (the three failing examples) and `:79-97,112-124,165-183` (the cross-team examples, already expecting 404 and already green); `spec/security/authorization_disclosure_spec.rb:95-106` — the normative rule in this repository, in its own words: *"somebody inside the Team who merely lacks the role must get FORBIDDEN, not NOT_FOUND"*; `spec/requests/environments_spec.rb:50-68,108-121` (the M01-11 precedent: in-team VIEWER → `:forbidden`, twice); `app/controllers/services_controller.rb:94-117,163-170` (`update` passes `params.dig(:service, :expected_revision)` through untouched; `render_error_response` hand-rolls `result.code == "CONFLICT" ? :forbidden : :unprocessable_entity`, copied from `app/controllers/environments_controller.rb:133`); `app/commands/update_service_desired_state.rb:56,60-63` (insufficient role emits code `CONFLICT`; the revision guard compares `expected_revision != service.desired_revision`, String `"1"` against Integer `1` — the failure body `{"current_revision":1,"expected_revision":"1"}` is that comparison); `app/commands/create_service.rb:69` and `app/queries/service_runtime_view.rb:22-27` (the same `CONFLICT`-for-insufficient-role code, twice more); `app/controllers/concerns/error_envelope.rb:17-32` (the canonical doc 09 §28 map — `FORBIDDEN` → 403, `CONFLICT` → 409 for a resource whose *state* refuses the request, unlisted codes defaulting to 422 rather than "something optimistic"); `docs/architecture/09-data-model-apis-contracts.md:697-708` (`NOT_FOUND`, `FORBIDDEN`, `REVISION_CONFLICT` are three distinct codes); `docs/implementation/M01/boundaries.yml:696-760` (all five files below are declared under `M01-12`; `error_envelope.rb` is **not**, it belongs to `M01-07` at `:364`); `docs/implementation/M01/stories/M01-12-service-desired-state.md:22,45,74` (AC4); `docs/implementation/M01/tasks.json:236-245` (`in_progress`, `attempts: 3`); `docs/implementation/M01/reports/M01-12.md:24` (AC7 still reads **PASS**); `docs/implementation/M01/DECISIONS.md:26-38` (the prior `M01-12` arbitration).

Verdict:   FIX

Action:    Raise the `M01-12` attempt budget by exactly one — the fourth and last — and spend it on five changes, all inside the Story's declared boundary, nothing else reopening. (1) `app/commands/update_service_desired_state.rb:60` coerces `expected_revision` at the boundary before comparing (`Integer(expected_revision)`, a malformed value being `VALIDATION_ERROR`, not a conflict) — this is the whole of failure 2 and a real lost-update defect: today *every* well-formed update from the browser is refused as a conflict while a caller who omits the field is let through. (2) The three producers of the insufficient-role failure — `update_service_desired_state.rb:56`, `create_service.rb:69`, `service_runtime_view.rb:24` — emit `"FORBIDDEN"` instead of `"CONFLICT"`, the code doc 09 §28 already has for this and the code `SuspendTeamMember` and `GrantInstanceRole` already use. (3) `services_controller.rb:164` stops testing one string and maps `FORBIDDEN` → 403, `NOT_FOUND` → 404, `REVISION_CONFLICT` → 409, defaulting to 422 — in the controller, **not** in `app/controllers/concerns/error_envelope.rb`, which `M01-12` may not touch. (4) `spec/requests/services_spec.rb:76` expects `:forbidden` and `:162` expects `:conflict`; each corrected expectation must gain an assertion it does not have today — line 76 that the response body discloses neither the Environment's name nor its slug, and line 162 that the body carries `code` `REVISION_CONFLICT` **and** that `service.reload.replicas` is unchanged. The statuses move; what the examples prove gets stronger, and the cross-team examples keep their 404 untouched. (5) The same commit finally applies item (4) of the arbitration of `DECISIONS.md:34` — `reports/M01-12.md:24` still reports AC7 as PASS, which that decision already ruled it may not. Gate evidence must record the stash-at-HEAD comparison the lead already ran; the 11 failures in `spec/gates/gate_scripts_spec.rb`, `spec/security/production_logging_spec.rb` and `spec/gates/unscoped_tenant_query_spec.rb:104` are not `M01-12`'s and are not touched in this round.

Reason:    A fourth attempt is justified by something specific, not by hope: the failure *class* changed. Attempts 1 and 2 were drowning in suite-wide pollution (30, then 29, from one non-transactional spec leaking committed rows across six unrelated files); attempt 3 closed that and left three failures that are now individually diagnosed to one line each, with the runtime evidence naming the cause — `expected_revision:"1"` against `current_revision:1` is a type at a boundary, not a design that needs rethinking. Under the convergence rule these are new findings, not the unresolved original, and `AGENT_RULES` grants another attempt to an informative new failure. DEBT is unsafe because `M01-13` writes the `Operation` and `OutboxEvent` for this exact mutation and `M01-18` reads the state it persists: shipping an update path that refuses every legitimate revision-checked write would make the next two Stories build on, and encode, a broken compare-and-swap — the "later Milestone builds directly on it" case that defaults to FIX. BLOCK is unjustified: no product choice is open, every answer here is already fixed by an artifact in the repository, and the remaining Stories are unaffected. On the two expectation edits I want the record explicit, because "make the test match the code" is the shape of the thing I am forbidden to authorise: I am not deciding the assertions were too strict — I am deciding they contradicted `authorization_disclosure_spec.rb:95-106` and the `M01-11` precedent, in the *loosening* direction for line 76 (404 for a member who can legitimately `GET` the resource tells a VIEWER the Service does not exist) and in the *wrong-axis* direction for line 162 (a stale revision is not an authorization failure). Both cases keep proving what AC4 and AC11 require, plus one assertion each that they did not carry before. Nothing here edits a gate, a threshold or a boundary, and `error_envelope.rb` stays out of the diff precisely because `boundaries.yml` gives it to another Story. If the fourth attempt does not turn `gate:local` green, the budget is spent and `M01-12` returns as a `BLOCK` candidate, not another raise.

Inherits:  M01 (milestone verdict) — the 11 failures the lead proved pre-existing at HEAD are Milestone-level debt of earlier Stories, to be carried in the ledger and reproduced in the `bin/milestone-pr` body; lifting `REVISION_CONFLICT` into `ErrorEnvelope::STATUSES`, so the code→status map lives in one place, inherits to `M01-13`, which is the next Story to produce that code and owns the shared concern's next legitimate edit.
