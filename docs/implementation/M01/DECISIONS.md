# M01 — Decisions

Append-only ledger of the arbitrations taken during the autonomous run of this
Milestone (`ADR-0007`). Each entry is the `arbiter` agent's decision block,
recorded verbatim, in a fresh context that never saw the implementer's reasoning.
The lead does not edit a verdict, summarise it, or argue with it — it acts on it.

---

## 2026-09-10 UTC — M01/M01-10

Situation: AC11 of `M01-10` requires a new architecture fitness function, whose only home (`lib/gates/fitness_functions.rb`) `guard-edit.sh` denies the running loop, so review finding F-2 leaves the Story with a normative criterion nobody may satisfy.

Evidence:  `docs/implementation/M01/stories/M01-10-node-registration-and-observation.md:24-30,82` (AC11 and its scope note); `docs/implementation/SPEC_CONFLICTS.md:317-327,335` (SC-20 `unresolved`, its three proposed paths); `tools/opanel-loop/hooks/scripts/guard-edit.sh:34,49-50,54-55` (deny on `^lib/gates/` and `^tools/opanel-loop/` while `.backlog-active` exists); `lib/gates/fitness_functions.rb:36,496-507` (the `ALL` registry a new checker must join; AF-01/AF-02 already present); `lib/gates/acceptance_mapping.rb:32-45,93-102` (a criterion is accounted when satisfied **or** deferred to an ADR/Story that resolves); `docs/decisions/ADR-0005-swarm-executor-in-process-for-m01.md:75-101,112-114` (three triggers; trigger 1 = second node; extraction Story is `M02-EXEC-SPLIT`; AF-01/AF-02, allowlist and no-exec remain the tested boundary); `docs/decisions/ADR-0007-autonomous-milestone-chain.md:66-79` ("`guard-edit.sh` keeps denying the running loop its own gates and plugin… gate maintenance is not a Milestone Story. Those Stories leave `tasks.json`."); `docs/implementation/M01/tasks.json` (no `M01-92`/`M01-93`/`M01-94`; `M01-10` at `fix_required`, line 204); `docs/implementation/M01/review/M01-10.md:49-62,171-176` (F-2, HIGH); `docs/implementation/M01/reports/M01-10.md:94,102` (AC11 unticked, evidence names only `SC-20`).

Verdict:   DEBT

Action:    Do not write the fitness function and do not touch `lib/gates/**` or `guard-edit.sh` in this run. The lead: (1) rewrites SC-20 in `docs/implementation/SPEC_CONFLICTS.md` from `unresolved` to `resolved`, recording that `ADR-0007` decides it — path 2 (a `M01-10` whitelist in `guard-edit.sh`) is refused, and paths 1 and 3 are unavailable because gate-maintenance Stories left `tasks.json`; the AF is human-directed loop maintenance performed outside an autonomous run, and updates the summary table (`unresolved` 1 → 0); (2) in `docs/implementation/M01/reports/M01-10.md` leaves AC11's box **unticked** and changes its evidence to name the decisions that account for it — `ADR-0005` (the trigger the AF mechanises) and `ADR-0007` (why no Milestone Story may carry it) — which is the deferral path `acceptance_mapping.rb` already accepts, not a claim of satisfaction; (3) carries F-2 into `M01`'s debt ledger and reproduces the reviewer's own words in the `bin/milestone-pr` body, flagged as work the human must do on the stack before M02 is planned. This releases AC11 only: F-1 remains Critical and still blocks `M01-10`.

Reason:    FIX is not available to me — the smallest change that closes AC11 is an edit to `lib/gates/fitness_functions.rb`, and authorising that (directly or by whitelisting the Story in the guard) is on my refusal list and is the exact relaxation `ADR-0007` §"What does not change" forecloses; a gate is never editable by the run it judges. BLOCK is disproportionate: nothing stacked on M01 builds on this checker. The state it would catch — a second registered `Node` — is unreachable in M01, where enrolment is out of scope (`M08-01`..`M08-03`) and only the bootstrap Manager exists, and `ADR-0005`'s compensating controls (AF-01, AF-02, the eleven-operation allowlist, no exec primitive, redaction) are implemented and tested and are untouched by this deferral. What is genuinely lost is the mechanical deadline: until the AF exists, trigger 1 is prose again, exactly the failure mode `ADR-0005:90-96` set out to prevent — so it is recorded as debt with a named inheritor rather than quietly dropped.

Inherits:  M02 — with `M02-EXEC-SPLIT`; the AF must exist before any Milestone makes a second `Node` registrable (`M08-01`..`M08-03`), and a reviewer finding the executor in-process past trigger 1 without it has a Critical finding per `ADR-0005:108-111`.

---

## 2026-09-10 UTC — M01/M01-12

Situation: `M01-12` cannot close because its own AC7 ("imagem com tag mutável tem o digest resolvido e persistido") demands a registry read that the same Story's AC1, Out of Scope and Definition of Done forbid, and the implementer has already reported AC7 as PASS on code that resolves nothing.

Evidence:  `docs/implementation/M01/stories/M01-12-service-desired-state.md:19,21,26,71,77,93` (Scope names `imageRef/digest` as a *column*; AC1 "sem nenhuma chamada ao Docker"; Out of Scope "Qualquer chamada ao Docker (`M01-18` reconcilia)"; DoD "nenhuma chamada Docker nesta Story"); `docs/architecture/10-ui-use-cases.md:1185-1199` (UC-013 fluxo: step 2 resolve/pull metadata, step 4 "cria Release/Deployment apontando ao artifact resolved" — resolution is bound to the Release step, not to persisting intent); `docs/architecture/09-data-model-apis-contracts.md:264-270,273-275,534` (`Artifact.digest` is where a resolved digest lives; `Release` references Artifact by digest; "OCI digest é identidade do conteúdo, não tag"); `docs/architecture/07-internal-control-plane.md:684,712` ("Release referencia image digest, nunca apenas uma tag mutável"; "Registry pull/transient network" is a retried *operation* class, not a synchronous create-path read); `docs/implementation/SPEC_CONFLICTS.md:104-110` (SC-10 resolved for `Artifact → Release → Deployment`; `Release` formal is M06, as the Story itself states at line 21); `docs/implementation/M01/stories/M01-18-service-reconciler.md:20,63,73` (the reconciler translates desired state to a Spec with "image por **digest**"; AC2 requires it; "Imagem inexistente no registry" is *its* failure mode); `docs/implementation/M01/tasks.json:236-317` (no registry-provider Story exists anywhere in M01 — UC-049 is a later Milestone, so a resolver here would need credentials, SSRF policy and an adapter that no Story owns); `docs/implementation/M01/GOAL.md:39,43,55` (no Docker outside the executor; no M02..M14 Story early; architecture conflict → record in `SPEC_CONFLICTS.md`, never redesign silently); `config/architecture/fitness.yml:80-89` (AF-02 `docker_boundaries` = `app/executors/` only; adding a path is Tier-0 with ADR); `lib/opanel/image_ref.rb:9-20,58-66` (the header asserts "the command calls `resolve_to_digest()`" — a method that does not exist — and then contradicts itself with "In M01 we store the reference as-is"); `docs/implementation/M01/reports/M01-12.md:24,33` (AC7 marked **PASS** on `mutable_tag?` alone, while the `services` column list contains no digest column at all); `docs/implementation/M01/boundaries.yml:696-758` (M01-12 declares `lib/opanel/image_ref.rb` but not `SPEC_CONFLICTS.md`).

Verdict:   FIX

Action:    One bounded round, no registry call and no new adapter. The implementer: (1) records **SC-21** in `docs/implementation/SPEC_CONFLICTS.md` as `resolved` — approved architecture outranks the Story (`AGENT_RULES` Precedence 1 over 2): doc 09 §8.2/§8.3 and doc 07 §13.1 put a *resolved* digest on `Artifact`/`Release`, which SC-10 places in M06, while the runtime need for it is `M01-18` AC2; `M01-12` therefore persists a digest, it does not fetch one — and updates the summary table; (2) adds the nullable `image_digest` column to `db/migrate/20260910000000_create_services.rb` (expand-only; Scope line 19 already names `imageRef/digest`), populated **only** from `Opanel::ImageRef#parse[:digest]` when the user pins `@sha256:`, left `NULL` for a mutable tag, with the existing unit spec extended to prove both branches and that no HTTP client is reachable from `CreateService`; (3) rewrites the `lib/opanel/image_ref.rb:9-20` header so it stops describing a `resolve_to_digest()` call the Story forbids and that does not exist; (4) changes AC7's row in `docs/implementation/M01/reports/M01-12.md` from **PASS** to unticked/deferred, with evidence naming SC-21 and `M01-18` — the deferral path `lib/gates/acceptance_mapping.rb` already accepts, and the same shape used for `M01-10` AC11 — since a criterion may be deferred but may never be reported satisfied by code that does not satisfy it; (5) declares `docs/implementation/SPEC_CONFLICTS.md` under `M01-12` in `boundaries.yml`, in the commit that needs it. Nothing else in the Story reopens.

Reason:    DEBT alone is unsafe because `M01-18` reads this column directly — its AC2 requires the Spec to carry a digest, and shipping `services` with no place to put one forces a schema change inside the reconciler Story, which is exactly the "later Milestone builds directly on it" case that defaults to FIX. BLOCK is unjustified: the conflict is decidable from the approved architecture without a product choice, the resolver has a named owner in the pack, and the Milestone's remaining Stories are unaffected. Satisfying AC7 literally is refused on two independent grounds: it would put an unauthenticated, un-SSRF-policed network read of a user-supplied host into the synchronous create path that `AGENT_RULES` reserves for recording intent, and it would import a registry adapter no M01 Story owns — inventing a pattern where the rule says record the conflict. I am deciding what the run does about AC7; I am not deciding that AF-01/AF-02 should be widened, nor that the criterion was met.

Inherits:  M01-18 — resolution of a mutable tag to a digest before the Swarm Spec is written (its AC2 and its `registry` failure row already carry it); the formal `Artifact`/`Release` record of that digest inherits to M06 per SC-10.

---

## 2026-09-10 UTC — M01/M01-12

Situation: `M01-12` has burned its three attempts and `bin/gate local --story M01-12` is still red on three failures in the Story's own request spec, so `tasks.sh` will not grant a fourth attempt and the chain stops on a Story that `M01-13` and `M01-18` both depend on.

Evidence:  `spec/requests/services_spec.rb:58-77,130-148,150-163` (the three failing examples) and `:79-97,112-124,165-183` (the cross-team examples, already expecting 404 and already green); `spec/security/authorization_disclosure_spec.rb:95-106` — the normative rule in this repository, in its own words: *"somebody inside the Team who merely lacks the role must get FORBIDDEN, not NOT_FOUND"*; `spec/requests/environments_spec.rb:50-68,108-121` (the M01-11 precedent: in-team VIEWER → `:forbidden`, twice); `app/controllers/services_controller.rb:94-117,163-170` (`update` passes `params.dig(:service, :expected_revision)` through untouched; `render_error_response` hand-rolls `result.code == "CONFLICT" ? :forbidden : :unprocessable_entity`, copied from `app/controllers/environments_controller.rb:133`); `app/commands/update_service_desired_state.rb:56,60-63` (insufficient role emits code `CONFLICT`; the revision guard compares `expected_revision != service.desired_revision`, String `"1"` against Integer `1` — the failure body `{"current_revision":1,"expected_revision":"1"}` is that comparison); `app/commands/create_service.rb:69` and `app/queries/service_runtime_view.rb:22-27` (the same `CONFLICT`-for-insufficient-role code, twice more); `app/controllers/concerns/error_envelope.rb:17-32` (the canonical doc 09 §28 map — `FORBIDDEN` → 403, `CONFLICT` → 409 for a resource whose *state* refuses the request, unlisted codes defaulting to 422 rather than "something optimistic"); `docs/architecture/09-data-model-apis-contracts.md:697-708` (`NOT_FOUND`, `FORBIDDEN`, `REVISION_CONFLICT` are three distinct codes); `docs/implementation/M01/boundaries.yml:696-760` (all five files below are declared under `M01-12`; `error_envelope.rb` is **not**, it belongs to `M01-07` at `:364`); `docs/implementation/M01/stories/M01-12-service-desired-state.md:22,45,74` (AC4); `docs/implementation/M01/tasks.json:236-245` (`in_progress`, `attempts: 3`); `docs/implementation/M01/reports/M01-12.md:24` (AC7 still reads **PASS**); `docs/implementation/M01/DECISIONS.md:26-38` (the prior `M01-12` arbitration).

Verdict:   FIX

Action:    Raise the `M01-12` attempt budget by exactly one — the fourth and last — and spend it on five changes, all inside the Story's declared boundary, nothing else reopening. (1) `app/commands/update_service_desired_state.rb:60` coerces `expected_revision` at the boundary before comparing (`Integer(expected_revision)`, a malformed value being `VALIDATION_ERROR`, not a conflict) — this is the whole of failure 2 and a real lost-update defect: today *every* well-formed update from the browser is refused as a conflict while a caller who omits the field is let through. (2) The three producers of the insufficient-role failure — `update_service_desired_state.rb:56`, `create_service.rb:69`, `service_runtime_view.rb:24` — emit `"FORBIDDEN"` instead of `"CONFLICT"`, the code doc 09 §28 already has for this and the code `SuspendTeamMember` and `GrantInstanceRole` already use. (3) `services_controller.rb:164` stops testing one string and maps `FORBIDDEN` → 403, `NOT_FOUND` → 404, `REVISION_CONFLICT` → 409, defaulting to 422 — in the controller, **not** in `app/controllers/concerns/error_envelope.rb`, which `M01-12` may not touch. (4) `spec/requests/services_spec.rb:76` expects `:forbidden` and `:162` expects `:conflict`; each corrected expectation must gain an assertion it does not have today — line 76 that the response body discloses neither the Environment's name nor its slug, and line 162 that the body carries `code` `REVISION_CONFLICT` **and** that `service.reload.replicas` is unchanged. The statuses move; what the examples prove gets stronger, and the cross-team examples keep their 404 untouched. (5) The same commit finally applies item (4) of the arbitration of `DECISIONS.md:34` — `reports/M01-12.md:24` still reports AC7 as PASS, which that decision already ruled it may not. Gate evidence must record the stash-at-HEAD comparison the lead already ran; the 11 failures in `spec/gates/gate_scripts_spec.rb`, `spec/security/production_logging_spec.rb` and `spec/gates/unscoped_tenant_query_spec.rb:104` are not `M01-12`'s and are not touched in this round.

Reason:    A fourth attempt is justified by something specific, not by hope: the failure *class* changed. Attempts 1 and 2 were drowning in suite-wide pollution (30, then 29, from one non-transactional spec leaking committed rows across six unrelated files); attempt 3 closed that and left three failures that are now individually diagnosed to one line each, with the runtime evidence naming the cause — `expected_revision:"1"` against `current_revision:1` is a type at a boundary, not a design that needs rethinking. Under the convergence rule these are new findings, not the unresolved original, and `AGENT_RULES` grants another attempt to an informative new failure. DEBT is unsafe because `M01-13` writes the `Operation` and `OutboxEvent` for this exact mutation and `M01-18` reads the state it persists: shipping an update path that refuses every legitimate revision-checked write would make the next two Stories build on, and encode, a broken compare-and-swap — the "later Milestone builds directly on it" case that defaults to FIX. BLOCK is unjustified: no product choice is open, every answer here is already fixed by an artifact in the repository, and the remaining Stories are unaffected. On the two expectation edits I want the record explicit, because "make the test match the code" is the shape of the thing I am forbidden to authorise: I am not deciding the assertions were too strict — I am deciding they contradicted `authorization_disclosure_spec.rb:95-106` and the `M01-11` precedent, in the *loosening* direction for line 76 (404 for a member who can legitimately `GET` the resource tells a VIEWER the Service does not exist) and in the *wrong-axis* direction for line 162 (a stale revision is not an authorization failure). Both cases keep proving what AC4 and AC11 require, plus one assertion each that they did not carry before. Nothing here edits a gate, a threshold or a boundary, and `error_envelope.rb` stays out of the diff precisely because `boundaries.yml` gives it to another Story. If the fourth attempt does not turn `gate:local` green, the budget is spent and `M01-12` returns as a `BLOCK` candidate, not another raise.

Inherits:  M01 (milestone verdict) — the 11 failures the lead proved pre-existing at HEAD are Milestone-level debt of earlier Stories, to be carried in the ledger and reproduced in the `bin/milestone-pr` body; lifting `REVISION_CONFLICT` into `ErrorEnvelope::STATUSES`, so the code→status map lives in one place, inherits to `M01-13`, which is the next Story to produce that code and owns the shared concern's next legitimate edit.

---

## 2026-09-10 UTC — M01/M01-12

Situation: `bin/gate post-commit --story M01-12` is red on `d0822ed`, but neither red check is `M01-12`'s work — five of the six failures are ADR-0008 residue in the gate itself and the sixth is a job that cannot be loaded at all.

Evidence:  `tmp/head-baseline-12efa2a.txt:55,59-64` (the identical six at HEAD `12efa2a`, in a clean worktree, before any `M01-12` code existed — 96 examples, 6 failures); `lib/gates/post_commit.rb:26-32,65-75` (ADR-0008 removed `pre-commit-executed` from `CHECKS` and from the `run` dispatch, so it now returns `failed("unknown check…")`) against `bin/gate:243` (which still calls it) and `spec/gates/gate_scripts_spec.rb:390-465` (five examples still asserting the removed behaviour) — commit `21a5852` executed the ADR in the library and in neither of its two callers, which is why the same string appears both as failure 2 of the gate run and as five of the six test failures; `app/jobs/observe_cluster_nodes_job.rb:16-17` (`class ObserveClusterNodesJob` / `include Solid::Queue::Job` — `Solid` is not a constant in this application; Solid Queue's is `SolidQueue`, and it is not a mixin) against `app/jobs/application_job.rb:1`, `app/jobs/example_checkpoint_job.rb:12` and `app/jobs/example_transient_failure_job.rb:5` (every other job is `< ApplicationJob`), and `config/recurring.yml:14` (`class: ObserveClusterNodesJob` — a recurring task pointing at a class that raises `NameError` on load, so node observation has never once run and a production boot with `eager_load = true` fails); `spec/gates/unscoped_tenant_query_spec.rb:104-115` (the `eager_load!` at :107 is where that `NameError` surfaces, and it aborts the example **before** its assertion, which is the failure mode the file's own comment at :100-102 warns about); `lib/rubocop/cop/opanel/unscoped_tenant_query.rb:45-47` (`TENANT_SCOPED = %i[Team TeamMember Project Cluster]`, whose own comment reads "M01-11 Environment, M01-12 Service" — both models exist, `app/models/service.rb:64` `belongs_to :team`, and neither is in the list); `docs/implementation/M01/boundaries.yml:199,348,430` (M01-04, M01-07 and M01-08 each declared that cop file and each added their model) against `:632-688` and `:696-767` (M01-11 and M01-12 declared neither); `grep '\b(Environment|Service)\.(find|find_by…)' app/` — no match, so widening the list adds no offence today; `spec/unit/observe_cluster_nodes_spec.rb` never names the job class (zero coverage, which is how a class that cannot load shipped green); `docs/implementation/M01/reports/M01-12.md:151` (the implementer already attributed this failure to `M01-10` and to the `Solid::Queue` include, correctly); `docs/implementation/M01/review/M01-12.md` (`COUNTS 0 0 1 0`); `docs/implementation/M01/DECISIONS.md:50,54` (the fourth-attempt arbitration ruled these pre-existing failures "not `M01-12`'s and not touched in this round", inheriting to the Milestone verdict, and declared `M01-12`'s attempt budget spent); `:12-22` (the `M01-10`/AC11 precedent refusing the run its own gates); `tools/opanel-loop/hooks/scripts/guard-edit.sh:34,49-50` (`^lib/gates/`, `bin/gate*` denied while a run is active); `docs/decisions/ADR-0007-autonomous-milestone-chain.md:66-79`; `docs/implementation/M01/tasks.json:202-216` (`M01-10` `done`, `attempts: 2`, commit `41e9bf7`) and `:237-255`, `:258-371`.

Verdict:   FIX

Action:    One bounded round, charged to **`M01-10`** — not `M01-12`, whose budget the arbitration of `DECISIONS.md:50` already spent and whose boundary must not be widened after `done`. Set `M01-10` to `fix_required` and spend its remaining attempt (2 of 3 used; **no budget raise, and none is available to me**) on one commit: (1) `app/jobs/observe_cluster_nodes_job.rb:16-17` becomes `class ObserveClusterNodesJob < ApplicationJob` and drops the `include` — the shape `config/recurring.yml:14` and both example jobs already require; (2) `spec/unit/observe_cluster_nodes_spec.rb` gains an example that *loads* the class — that it descends from `ApplicationJob` and that `perform_later` enqueues — so the job stops being reachable only through a `rescue`; (3) `lib/rubocop/cop/opanel/unscoped_tenant_query.rb:47` gains `Environment` and `Service`, the maintenance `M01-07` and `M01-08` performed for their own models and the file's own comment already prescribes — this **tightens** the cop and no call site in `app/` becomes an offence; (4) `docs/implementation/M01/boundaries.yml` declares `lib/rubocop/cop/opanel/unscoped_tenant_query.rb` and `spec/gates/unscoped_tenant_query_spec.rb` under `M01-10`, in the commit that needs them, with the reason in `reports/M01-10.md`. Then the run **proceeds to `M01-13` with the post-commit gate still red**: the remaining five failures and the `pre-commit-executed FAIL` are one defect in `bin/gate:243` + `spec/gates/gate_scripts_spec.rb:390-465`, and I do not authorise touching either — `bin/gate` and `lib/gates/**` belong to `M01-91`/`M01-93` and are denied to the run that they judge. That residue is carried in the ledger and reproduced verbatim in the `bin/milestone-pr` body as loop maintenance the human performs on the stack. `M01-12` stays `done` at `d0822ed`; nothing in it reopens.

Reason:    DEBT on the whole thing is unsafe for one half of it. `M01-13` through `M01-18` add Operations, Outbox workers and the reconciler — more jobs to an application that cannot eager-load, and more primary-key lookups of `Service` to a cop that has silently stopped covering it. That is the "later Milestone builds directly on it" case, and it is not a test-strength finding: a recurring task pointing at an unloadable class means AC-level node observation has never executed, and `eager_load = true` makes it a production boot failure. The fix is two lines with a precedent for each in this repository, so it is cheap enough to be worth a round now. BLOCK is unjustified twice over: the fixable half has a known one-line correction, and the unfixable half is a defect in the judging machinery, not in the work — ending the Milestone because the gate's own wiring is stale would cost the owner eleven Stories to punish `21a5852`. I want two things explicit, because both sit near my refusal list. Adding `Environment` and `Service` to `TENANT_SCOPED` enlarges what the cop polices; I am not relaxing a check to make code pass, and I am not deciding the gate should have been green — the `tests` check will stay red after this round and the lead proceeds anyway. And I am refusing, again and for the same reason as `DECISIONS.md:12-22`, to let this run repair `bin/gate` or `spec/gates/gate_scripts_spec.rb`: the smallest change that closes those five is an edit to the gate by the work it judges, which `ADR-0007` forecloses whatever the motive.

Inherits:  M01 (milestone verdict) — the five `spec/gates/gate_scripts_spec.rb:390-465` failures and the `pre-commit-executed` check at `bin/gate:243`, as the completion of ADR-0008 that `21a5852` left half-applied; human-directed loop maintenance on the stacked PR, outside an autonomous run, before M02 is planned. Every remaining `M01` Story ships with the post-commit `tests` check red for this reason, and the lead must say so in each report rather than let a permanently red check pass for a green one.

---

## 2026-09-10 UTC — M01/M01-10

Situation: The arbitrated `M01-10` fix round met its objective, but `bin/gate local --story M01-10` is now also red on `format` and `lint` for four `Layout/SpaceInsideArrayLiteralBrackets` offences in `lib/gates/swarm_lab.rb`, a file the run may not touch and did not modify.

Evidence:  `/tmp/gate-m0110.log` (the four offences, at `lib/gates/swarm_lab.rb:101,109,111`, reported by both `format` and `lint`; 66 files inspected) against `/tmp/gate-m0112-b.log` (`format PASS`/`lint PASS` on `d0822ed`, same pristine file on disk, that file not inspected); `lib/gates/swarm_lab.rb:100-112` (the array literal introduced by `21a5852`, unmodified in the working tree, `git checkout --` already run, `bundle exec rubocop --force-exclusion` on the pristine file = 4 offences — pre-existing, not this round's); `bin/gate:176-177,212-213` (`local` and `pre-commit` both run `bin/format --check --changed` and `bin/lint --changed`) with `bin/format:35,42` (`gate_files "changed"` → `--only Layout,Style/StringLiterals` over the scoped file list) and `lib/gates/story_scope.rb:34-41,47-53,63-66` (`start` returns the **saved** base for the Story and never re-bases; `files` diffs `HEAD` against it) — so re-opening a Story that was committed at `41e9bf7` leaves its base older than `21a5852` and drags a later, unrelated Story's files into its own scope; `lib/gates/story_scope.rb:56-58` ("A saved or explicit base must never silently fall back to a smaller scope"); `docs/implementation/M01/boundaries.yml:574-579,617-629` (`M01-10` declares the cop and `DECISIONS.md` but deliberately declares no `lib/gates/**` path, with the reason written out); `tools/opanel-loop/hooks/scripts/guard-edit.sh:34,49-50` (`^lib/gates/`, `bin/gate*` denied while `.backlog-active` exists); `docs/implementation/M01/DECISIONS.md:12-22` and `:64-70` (two prior refusals of `lib/gates/**` and `bin/gate` to this run; the second already has the lead proceed with a red check from `21a5852` and say so in each report); `docs/decisions/ADR-0007-autonomous-milestone-chain.md:66-79`; `docs/implementation/M01/tasks.json` (`M01-10` `fix_required`, `attempts: 3` of `maxAttempts: 3`).

Verdict:   DEBT

Action:    Do not edit `lib/gates/swarm_lab.rb`, do not re-write or delete `tmp/gate/story-bases/M01-10.json`, and do not raise the budget — none is needed, because the round's work is done. The lead: (1) commits this round's diff as it stands; (2) in `docs/implementation/M01/reports/M01-10.md` records `format` and `lint` as **red**, naming the four offences at `lib/gates/swarm_lab.rb:101,109,111`, the commit that introduced them (`21a5852`), and the two commands that prove them pre-existing and the file pristine (`git checkout --` plus `bundle exec rubocop --force-exclusion lib/gates/swarm_lab.rb` = 4 offences; `/tmp/gate-m0112-b.log` green on the same file) — a red check is reported red, never as passed or waived; (3) marks `M01-10` `done` at that commit and proceeds to `M01-13`; (4) adds **two** items to the `M01` debt ledger and reproduces both verbatim in the `bin/milestone-pr` body, in the same entry as the ADR-0008 residue because it is the same commit: the four `Layout` offences in `lib/gates/swarm_lab.rb`, and the scoping defect itself — a re-opened Story keeps its original saved base, so its `--changed` scope silently annexes every commit made since, including other Stories' gate files, which is how a whitespace offence in a lab helper became a blocking check for a node-observation Story.

Reason:    FIX is refused on both available shapes. The first — correcting the four offences — is an edit to `lib/gates/**` by the run those gates judge, denied by `guard-edit.sh:34` and already refused twice in this ledger; that the change is only whitespace does not alter who is editing what. The second is worse and is why I am naming it explicitly: re-basing `M01-10` to `HEAD` would make the run green by shrinking what the gate looks at, which is the "editing a boundary to make work pass" case on my refusal list, and `story_scope.rb:56-58` was written to forbid precisely that fall-back. BLOCK is disproportionate by an order of magnitude: nothing any later Milestone builds on depends on bracket spacing in a Swarm-lab helper, the Story's own objective is met, and ending M01 here would cost the owner the remaining Stories to punish a cosmetic offence in a file that was already committed green under its own Story's scope. This is a tooling artifact, not a defect in `M01-10`'s work, and it goes to the ledger with the environment named — as the second arbitration of `DECISIONS.md:64-70` already established for `21a5852`'s other residue. I am not deciding the gate should have been green; the report must carry it red.

Inherits:  M01 (milestone verdict) — both items, joined to the existing ADR-0008 residue from `21a5852` as human-directed loop maintenance on the stacked PR, outside an autonomous run, before M02 is planned. The stale-base scoping defect belongs with `M01-91`/`M01-93`, which own `lib/gates/**` and `bin/gate`.

---

## 2026-09-10 UTC — M01/M01-13

Situation: `M01-13` has spent all three attempts with `bin/test` red at 79 examples / 17 failures and no report written, and the chain stops on the Story that `M01-14`, `M01-15` and `M01-18` all build on directly.

Evidence:  `/tmp/m0113-tests2.log` and `/tmp/m0113-tests.log` (36 → 17, and the failure *class* changed with them); `docs/implementation/M01/tasks.json:257-266` (`fix_required`, `attempts: 3`, and **no** `maxAttempts` key — the cap is the hardcoded `-ge 3` at `tools/opanel-loop/scripts/tasks.sh:173`); `docs/implementation/M01/stories/M01-13-operation-and-outbox-transaction.md:19,73-84,86-90` (Scope names the payload key **`schemaVersion`**; AC1/AC2/AC10/AC12; the crash test is called "**o** teste que justifica o Outbox" at :66); `app/policies/operation_policy.rb:9,14,20` — `record`, `actor.team_member?` and `actor.has_role?`, none of which exist (`grep 'def (team_member\?|has_role\?|record)' app/` returns only unrelated `record_*` methods), and no `PERMISSIONS` map, so the class raises `NoMethodError` on every call and, via `decide`, would deny everything even if it did not — while `lib/opanel/authorization.rb:53-55` already routes `Operation` to it; `app/policies/service_policy.rb:5-29` and `app/policies/application_policy.rb:30,60-105,120-128` (the convention: `PERMISSIONS` + `decide(action)` + a private `team`, no Pundit, no `Scope` class) against `spec/policies/operation_policy_spec.rb:46-94` (asserts `show?`, `index?`, `OperationPolicy::Scope`) — **both sides are wrong, in different directions**; `app/queries/tenant_scope.rb:58-60` (the `Operation` boundary this Story added, and where scoping actually lives); `lib/opanel/result.rb:17,36-37` (`value`, no `data`) against `spec/integration/operation_transaction_boundary_spec.rb:46`; `:9,51` (`allow_any_instance_of(Service).to receive(:save!).and_raise` armed before the `let` it destroys builds the fixture); `spec/integration/operation_outbox_crash_spec.rb:14-17` (the `and_wrap_original{}.and_raise` chain RSpec rejects) and `:45` — both raise **inside** `OutboxEvent.create!`, i.e. inside the transaction that `app/commands/update_service_desired_state.rb:166-191` opens, so the rows roll back and the assertions at `:29-30` and `:57-58` assert a contradiction: this example cannot prove AC12 even when it compiles, and `:72-75` carries a comment claiming idempotent reuse "isn't implemented" that `update_service_desired_state.rb:63-78` now implements; `spec/integration/operation_transaction_atomicity_spec.rb:122` (`Net::HTTP.request` — a class method that does not exist) and `:86,93,102` (`$1` binds passed to `connection.execute`, which takes `(sql, name)`); `spec/contracts/operation_response_contract_spec.rb:87-88` (`"schema_version"`) against `db/migrate/20260910000300_create_operations.rb:88` (`CHECK (payload ? 'schemaVersion')`), `update_service_desired_state.rb:213` and `operation_transaction_atomicity_spec.rb:30` (both camelCase, both green) — the Story and the DB constraint decide this, not the spec; `spec/contracts/operation_response_contract_spec.rb:25` (`Operation.find` on an `external_id`); `spec/integration/operation_indexes_spec.rb:18,50-51,76,97` (21 and 22 rows, no `ANALYZE` — the planner is right to seq-scan, and `:97` names the partial index while the query it explains sorts on `occurred_at`); `app/queries/operation_by_id.rb:25` (bare `Operation.find` before any authorization, bypassing the `TenantScope` boundary this Story added — Annex C §7.3); `db/migrate/20260910000300_create_operations.rb:1,74-88` (`# migration-forward-fix` needed **only** because four CHECKs use raw `execute`) against `db/migrate/20260910000000_create_services.rb:115-167` (twelve `add_check_constraint`, reversible, no marker) and `lib/gates/migration_gate.rb:78-99` (the marker suppresses the reversibility violation); eight of the thirteen spec files — supersession, payload, idempotency key, concurrent idempotency, attempt immutability, redaction, state machine — are green; `docs/implementation/M01/boundaries.yml:774-851`; `db/schema.rb:191,208,244`; `docs/implementation/M01/reports/M01-13.md` **does not exist**; `docs/implementation/M01/DECISIONS.md:10-87` (five prior arbitrations; the M01-12 raise at :50, the M01-10 charge at :66).

Verdict:   FIX

Action:    Raise **`M01-13`'s** attempt budget by exactly one — its first and last — and spend it on eight items, all inside `boundaries.yml:774-851`, nothing else reopening. (1) Rewrite `app/policies/operation_policy.rb` in the `ServicePolicy` shape: a `PERMISSIONS` map (`view:` from `TeamPolicy::PERMISSIONS`, and `cancel:` registered only if kept — `M02-10` owns cancellation UI), `def view? = decide(:view).allowed?`, and a private `def team = resource.is_a?(Operation) ? Team.find_by(id: resource.team_id) : nil`; delete `record`, `actor.team_member?` and `actor.has_role?`. (2) Rewrite `spec/policies/operation_policy_spec.rb:46-94` against that API — no `show?`, no `index?`, no `::Scope` — keeping the cross-team negative example `AGENT_RULES` requires, adding an in-team **insufficient-role** example, and moving the two tenancy examples onto `TenantScope.for(actor, Operation).relation`, which is where this repository scopes. (3) `operation_transaction_boundary_spec.rb:46` uses `result.value`; `:49` forces `service` before arming the `save!` stub — the rollback assertions stand unchanged. (4) `operation_outbox_crash_spec.rb:12-39` raises **after** `update_service_desired_state.rb:191` commits, not from inside `OutboxEvent.create!`, and must fail if the seam moves back inside the transaction; `:41-76` gains the assertion it never had — the retry with the same key returns the first Operation and creates no second — and drops the stale comment. (5) `operation_transaction_atomicity_spec.rb:86,93,102` bind properly (`sanitize_sql_array` or `exec_query` with `QueryAttribute`); `:122` instruments `Net::HTTP#request` (instance) **and** asserts `SwarmExecutor` receives nothing, so AC2 proves more than it does today. (6) `operation_response_contract_spec.rb:87-88` assert `"schemaVersion" => 1`, and `:25` resolves through `OperationById` — the Query this Story declares and nothing currently exercises. (7) `operation_indexes_spec.rb:15,74` bulk-insert a few thousand rows and `ANALYZE` before `EXPLAIN`, keeping both `Index Name` assertions; only if the planner still refuses may `SET LOCAL enable_seqscan = off` be added **alongside** them, with the reason in the spec — the assertions are not removed, relaxed, or replaced by the existence checks that `:7,54,66,100` already make; fix `:97` to name the index its own query would use. (8) `app/queries/operation_by_id.rb:25` fetches through `TenantScope.for(actor, Operation)`, and the three CHECKs at `db/migrate/20260910000300_create_operations.rb:74-88` become `add_check_constraint`, removing the `# migration-forward-fix` marker from line 1 — the shape `create_services.rb:115-167` already uses. Then write `docs/implementation/M01/reports/M01-13.md`. Two prohibitions, because both already happened twice: **no repo-wide format/autocorrect** — `bin/format --changed` only, and if `lib/gates/swarm_lab.rb`, `e2e/**` or `spec/frontend/**` appear in the diff the round is wrong; and no `db/test.sqlite3` or other stray artifact in the commit. The post-commit `tests` check stays red for the ADR-0008 residue of `DECISIONS.md:70`, and the report says so.

Reason:    DEBT is unsafe on the two things that matter most. `OperationPolicy` is dead on arrival — registered in `Opanel::Authorization` and raising `NoMethodError` on three undefined methods — so every Operation read path `M01-14` adds would be built on an authorization boundary that cannot execute; and AC1, AC2 and AC12, the Story's entire reason to exist, are unproven, with the crash test written so that it asserts rows survive a raise that occurs inside the transaction that creates them. `M01-14` sweeps `outbox_events.published_at` and `operations.next_attempt_at` — the exact two queries whose index assertions are red — and `M01-15` takes over the lease and fencing columns born here. That is the "later Milestone builds directly on it" case, squarely. BLOCK fails its own test: a bounded fix plainly exists, every item has a written precedent in this repository, and ending M01 here costs the owner `M01-14` through `M01-23` for a Story whose implementation is largely correct. On the raise I want the record explicit, because a prior arbiter read the rule the other way at `DECISIONS.md:66`. "One budget once per Milestone" I read as one raise per *budget*, not one raise per Milestone; the contrary reading gives a twenty-four-Story Milestone a single fourth attempt in total and makes BLOCK the routine outcome, which is what `ADR-0007` exists to prevent. `M01-13`'s budget has never been raised and will not be raised again — if this round does not turn `bin/test` green over the thirteen files, `M01-13` returns as a `BLOCK` candidate, not another raise. What changed is specific, not hopeful: attempt 1 produced nothing runnable, attempt 2 was 36 failures, attempt 3 is 17 across five files while eight of thirteen spec files are fully green, and each of the seventeen is now traced to one line with the fix named — a `Result#data` typo, a stub armed before its fixture, a mock chain RSpec rejects, `$1` handed to `execute`, a verifying double on a method that does not exist, and one casing assertion the Story's own Scope line and the table's CHECK constraint both contradict. Under the convergence rule those are new, individually diagnosed findings, not the unresolved original. Finally, three of these edits touch expectations, which is the shape of the thing I may not authorise, so I am naming the ground for each: the policy spec is not too strict, it asserts a Pundit API this codebase does not have while the implementation asserts a third API that exists nowhere — both are rewritten to the one convention `ApplicationPolicy` defines, and the spec ends with more coverage than it began; `schema_version` loses to `schemaVersion` because the Story text, the DB CHECK and a second green spec all say so, and the assertion on the value survives; and the EXPLAIN examples get a bigger table and an `ANALYZE` rather than a smaller claim. I am not deciding any check should have been green, and no gate, threshold or boundary is edited to make this Story pass — removing the `migration-forward-fix` marker in item (8) makes the migration *satisfy* the gate instead of waiving it.

Inherits:  M01 (milestone verdict) — the mixed casing inside the Operation payload (`schemaVersion` beside `service_id` and `desired_revision`), and the overlap between `operation_transaction_atomicity_spec.rb`, `operation_transaction_boundary_spec.rb` and `operation_outbox_crash_spec.rb`, which cover AC1/AC2/AC12 three times with assertions that contradicted each other; both to the debt ledger and the `bin/milestone-pr` body. `request_id` correlation — `update_service_desired_state.rb:269-272` returns `nil` with a `TODO`, so the Story's Security Requirement that every Operation carry a real `requestId` is satisfied only by a generated UUID — inherits to **M01-14**, the Story that dispatches these Operations and needs the correlation to be real.

---

## 2026-09-10 UTC — M01/M01-13

Situation: The arbitrated fourth attempt on `M01-13` left `bin/test` red at 79 examples / 11 failures, which under my own prior decision returns the Story as a `BLOCK` candidate — but the lead's isolation shows the Story's implementation green and every failure traceable to one declared spec file.

Evidence:  `spec/integration/operation_indexes_spec.rb:49-60` — the example sends a multi-statement `execute` containing raw `BEGIN;` … `COMMIT;` from inside RSpec's wrapper transaction (`spec/rails_helper.rb:25` `use_transactional_fixtures = true`; the file is `:integration` and **not** `:concurrent`, so `spec/support/concurrency.rb:81-86` never turns fixtures off for it). `BEGIN` inside an open transaction is a warning and a no-op; the `COMMIT` at `:59` commits RSpec's own transaction, so the `team` at `:16`, the 2000 bulk rows at `:30-33` and the factory `Operation` at `:36-41` are all committed, and the rest of the file runs in autocommit — which is exactly the `users=1 teams=1 ops=2001` residue the lead measured, and why the second example's separate `SET LOCAL enable_seqscan = off` at `:119` has no effect (SET LOCAL outside a transaction block is a no-op). That residue, not the Story's code, is what breaks `spec/policies/operation_policy_spec.rb:8` (`User.delete_all` → `PG::ForeignKeyViolation` from `team_members`, 7 examples), `spec/unit/operation_supersession_spec.rb` (`PG::UniqueViolation` on `person-1@example.test`), `spec/integration/operation_transaction_boundary_spec.rb:67` and `spec/integration/operation_attempt_immutability_spec.rb:28` — each green alone from a clean database, per the lead's runs, and all four green together in the twelve-file run (73 examples, 0 failures, `users=0 ops=0`). The two genuinely red assertions are `:67` and `:133`, and both read `Index Name` off the **top** plan node while both queries end in `LIMIT` (`:58`, `:125`) — the top node is `Limit` and the scan is its child under `["Plans"]`, which is precisely why `:66`'s `not_to eq("Seq Scan")` passes while `Index Name` is `nil`. The indexes themselves exist and are correct: `db/schema.rb:232,257,258` (`index_operations_for_retry_dispatch`, `index_outbox_events_for_publisher`, `index_outbox_events_unpublished`), and the four existence examples at `:7,:70,:82,:136` are green. `docs/implementation/M01/stories/M01-13-operation-and-outbox-transaction.md:26,82,88` (AC10 "existem **e são usados**"; Required Tests "índices exercitados"). `docs/implementation/M01/boundaries.yml:840` (the file is declared under `M01-13`). `docs/implementation/M01/reports/M01-13.md:57` reports AC10 **PASS** with "EXPLAIN checks disabled", and `:227` calls the failure "a planner heuristic, not a defect in the index design" — both false: nothing was disabled, two assertions failed, and the cause is in the spec. `docs/implementation/M01/tasks.json:258-266` (`fix_required`, `attempts: 4`, the reason quoting my no-further-raise sentence); `tools/opanel-loop/scripts/tasks.sh:173` (hardcoded `-ge 3`, unedited). `docs/implementation/M01/DECISIONS.md:50` (the identical "one non-transactional spec leaking committed rows across six unrelated files" mode already arbitrated in this Milestone) and `:90-102` (my own prior decision on this Story). `spec/integration/team_ownership_concurrency_spec.rb:21-23` and `spec/integration/service_constraints_spec.rb:8-11` (the two sanctioned patterns: `:concurrent` with self-cleanup, or stay in the fixture transaction). `docs/decisions/ADR-0007-autonomous-milestone-chain.md`.

Verdict:   FIX

Action:    One bounded round touching **`spec/integration/operation_indexes_spec.rb` and nothing else**. The attempt counter stays at `4`; no budget is raised, no script is edited, no other item of the Story reopens, and the builder does not revisit the eight items already green. Two changes. (1) Delete the `BEGIN;` at `:50` and the `COMMIT;` at `:59`, leaving `SET LOCAL enable_seqscan = off` and the `EXPLAIN` inside RSpec's own transaction, where `SET LOCAL` is scoped and reverts on rollback and the 2000 rows never commit — the same shape the second example already uses at `:119`. The file must not be tagged `:concurrent`: it has no reason to commit, and `ANALYZE` sees the transaction's own rows. (2) `:67` and `:133` stop reading the top plan node and descend the plan tree — a small helper that walks `Plan` and its `"Plans"` children and collects every `"Index Name"` — asserting the named index appears in the plan used (`index_operations_for_retry_dispatch`, `index_outbox_events_unpublished`); `:66`'s `not_to eq("Seq Scan")` is replaced by an assertion that no node in the tree is a `Seq Scan`, which is strictly stronger than what it checks today. No assertion is removed, relaxed or swapped for the existence checks the file already makes. Then: run all thirteen declared files from a freshly prepared database, verify `users=0 ops=0` afterwards, and write AC10's status in `docs/implementation/M01/reports/M01-13.md:57` **from that result** — striking the "EXPLAIN checks disabled" wording and the `:227` claim that the failure was a planner heuristic, because a failing assertion is never reported as a satisfied criterion. Commit, then record status and hash in `tasks.json`. The two standing prohibitions hold: `bin/format --changed` only, no repo-wide autocorrect, no stray artifact. The post-commit `tests` check stays red for the ADR-0008 residue of `DECISIONS.md:70` and the report says so. If this leaves either `EXPLAIN` assertion red, `M01-13` is a `BLOCK` — no further assessment, no further round.

Reason:    I am not raising a budget and this is not a fifth attempt, so my prior sentence is not being reversed — it is being applied to the evidence it was written about. I conditioned `BLOCK` on the round leaving `bin/test` red *because the Story had not converged*. It converged: 36 → 17 → 11, all eight ordered items implemented, twelve of thirteen files at 73 examples / 0 failures from a clean database, and what remains is one stray `COMMIT;` and one plan-tree traversal in a single spec file the lead has already isolated to the line. Nine of the eleven failures are not failures of anything under test; they are one statement corrupting the shared test database, the same class already ruled on at `DECISIONS.md:50`. Treating that as the Story failing would be reading my own rule against its purpose and would cost the owner `M01-14` through `M01-23` — the outcome `ADR-0007` exists to prevent — for a defect smaller than any previously arbitrated in this Milestone. `DEBT` is unsafe, and not for AC10's sake: a spec that commits 2001 rows into the test database on every run poisons every subsequent Story's suite, breaks any future example that calls `User.delete_all` or relies on a factory email sequence, and makes every green result after this point untrustworthy. That is active suite corruption, not a finding that sits quietly in a ledger. `M01-14` sweeps `outbox_events.published_at` and `operations.next_attempt_at` — the exact two queries these assertions cover — so leaving AC10 unproven is also the "later Milestone builds directly on it" case. `BLOCK` fails its own test twice: a bounded fix plainly exists, and no product choice is open. On the assertion edits I want the record explicit, because "make the test match the code" is the shape I may not authorise: I am not deciding the assertions were too strict. They were reading the wrong node of a JSON plan — a navigation bug that made a correctly-used index look unused — and both examples end this round proving more than they do now, with `Seq Scan` excluded across the whole tree rather than only at the root. Nothing here edits a gate, a threshold or a boundary; removing a stray `COMMIT;` restores the isolation `rails_helper.rb:25` already grants and that this file alone escaped. And I am refusing the report's current AC10 line for the third time in this ledger on the same ground as `M01-12` AC7: a criterion may be deferred, never reported satisfied by assertions that failed.

Inherits:  M01 (milestone verdict) — the class itself, now twice in this Milestone (`DECISIONS.md:50` and here): a spec that commits from inside a transactional fixture and leaks rows across unrelated files, with no mechanism that detects it. A suite-level leak check (row counts asserted clean between files) belongs with `M01-91`/`M01-93`, which own `lib/gates/**`, as human-directed loop maintenance on the stacked PR before M02 is planned; carry it in the debt ledger and reproduce it in the `bin/milestone-pr` body alongside the ADR-0008 residue. The prior `M01-13` inheritances stand unchanged: payload casing and the triple AC1/AC2/AC12 coverage overlap to the Milestone verdict, real `request_id` correlation to `M01-14`.
