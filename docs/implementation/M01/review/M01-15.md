**Story:** M01-15 — Resource Locks, Leases and Fencing Tokens
**Reviewer:** independent
**Date:** 2026-09-11 UTC

## Acceptance Criteria

All 10 acceptance criteria are **verified and met**:

| AC | Criterion | Evidence | Status |
|---|---|---|---|
| 1 | Mutation serialization with valid lease | `spec/integration/resource_lock_serialization_spec.rb` + `app/commands/acquire_resource_lock.rb` atomic INSERT...ON CONFLICT | PASS |
| 2 | Concurrent mutations serialized with barrier | Serialization test line 39-45 with explicit barrier; one acquires, one blocked until release | PASS |
| 3 | Fencing token monotonic per scope | Database constraint: `fencing_token = resource_locks.fencing_token + 1` under ON CONFLICT row lock; `spec/unit/fencing_token_spec.rb` | PASS |
| 4 | Stale token rejected (central test) | `spec/integration/resource_lock_fencing_spec.rb:25-63` — worker 1 token N rejected after worker 2 increments to N+1; raises `InvalidTransition` | PASS |
| 5 | Lease released after operation result | `ReleaseResourceLock` idempotent; tests show takeover only after release; guard at `operation.rb:185` blocks status change while lease_owner present | PASS |
| 6 | Successor revalidates before acting | `spec/integration/resource_lock_successor_revalidation_spec.rb:30-67` — fenced write refused if took_over=true but observation not recorded; line 426-429 of `operation.rb` enforces this | PASS |
| 7 | TTL guarantees release | `spec/integration/resource_lock_ttl_expiry_spec.rb` + `RecoverStalledOperations` orphaned lock release at line 209 | PASS |
| 8 | Concurrent reads not blocked | `spec/integration/resource_lock_concurrent_reads_spec.rb` — multiple readers execute while mutation locked | PASS |
| 9 | Events observable | Structured logs in `AcquireResourceLock`, `RenewResourceLock`, `ReleaseResourceLock` with scope_key, owner, fencing_token, team_id | PASS |
| 10 | Owner not from client | `Opanel::WorkerIdentity.current` at `acquire_resource_lock.rb:71`; `spec/security/resource_lock_owner_spec.rb:9-22` proves derivation | PASS |

## Boundary & Scope

**Declaration:** `docs/implementation/M01/boundaries.yml:937-990`
**Widening during work:** No scope creep. All changes within declared boundary.
**Assessment:** PASS — diff contains only declared files.

## Architecture Invariants

1. **Cluster-first:** Locks serialize Operations running on Docker Swarm services. N/A here; lock is database-only infrastructure.

2. **Desired State in PostgreSQL, Actual in Swarm:** ResourceLock is infrastructure state (Desired), persisted in PostgreSQL. Lease owner is observed, never assumed. M01-18 reconciler will observe Actual State.

3. **MCP path:** No MCP involved in M01-15. M01-18 will use locks acquired via Command → Operation → Reconciler → Executor.

4. **Socket isolation:** ResourceLock code does not touch `/var/run/docker.sock`.

5. **Secrets opaque:** No secrets in ResourceLock; worker_identity is public.

6. **One-transaction writes:** `acquire_resource_lock.rb:109-126` is one atomic INSERT...ON CONFLICT...RETURNING. No network call inside transaction.

**Assessment:** PASS

## Security

1. **Authorization server-side:** `ResourceLock` belongs to Team (foreign key, `team_id` required at line 566 of migration). TenantScope enforces it; `acquire_resource_lock.rb` takes team as parameter. No client-supplied owner.

2. **Owner never from client:** `Opanel::WorkerIdentity.current` (line 71) derived from process environment. Test `resource_lock_owner_spec.rb:9-22` proves this. No `owner` parameter accepted by caller.

3. **Secrets absent:** No secrets persisted; worker identity is a string identifier.

4. **Untrusted input validated:** `scope_key` trimmed and validated for emptiness (line 67). `ttl_seconds` validated > 0 (line 68).

5. **SSRF:** N/A

**Assessment:** PASS

## Test Strength & Weakened Gates

1. **AC4 central test (fencing):** `resource_lock_fencing_spec.rb:25-63` — removes the fencing check at `operation.rb:161` would cause this test to fail. Test is **not vacuous**; it asserts both that stale token is rejected (old_token case, raises) and correct token is accepted (new_token case, succeeds, line 89).

2. **Guard enforced:** `operation.rb:178-190` callback raises `InvalidTransition` if status changes while `lease_owner.present?` without `@_fencing_token_valid = true` flag. Test `resource_lock_fencing_spec.rb:92-113` verifies guard. No bypass found.

3. **AC6 enforcement:** Line 426-429 of `operation.rb` raises if `lock&.took_over? && !lock.observation_recorded?`. Test `resource_lock_successor_revalidation_spec.rb:30-67` proves this. Observation check is **enforced in code**, not documented-only.

4. **Spec cleanup:** Both `resource_lock_serialization_spec.rb` and `resource_lock_concurrent_reads_spec.rb` use proper cleanup ordering (ResourceLock → TeamMember → Team inside one transaction, no rescue). Pattern matches precedent at `team_ownership_concurrency_spec.rb:65-72`. No leaked rows.

5. **Test isolation:** Each concurrent test uses unique_namespace + SecureRandom.hex to avoid duplicate key conflicts. All M01-15 tests green (1118 tests, 0 failures, exit_status 0, timestamp 2026-09-11T05:04:04Z).

6. **No gate weakening:** No lint rule turned off, no example excluded, no lowered thresholds. `bin/gate local --story M01-15` passes per report line 76.

**Assessment:** PASS

## Critical Implementation Details Verified

| Item | Location | Evidence |
|---|---|---|
| `took_over` from RETURNING | `acquire_resource_lock.rb:125,141,145` | `(xmax <> 0) AS took_over` captured from SQL result; checked against `== "t" \|\| == true`; excluded from `instantiate` call |
| Fencing token used correctly | `operation.rb:161,421-423` | Reads from parameter; compares `self.fencing_token != fencing_token`; raises if mismatch |
| Unscoped find removed | `acquire_resource_lock.rb:145` | Uses `ResourceLock.instantiate(row_hash.except("took_over"))` instead of redundant lookup; no unscoped `find` |
| Cop compliance | `lib/rubocop/cop/opanel/unscoped_tenant_query.rb:47` | ResourceLock registered in TENANT_SCOPED; no unjustified suppression in recover_stalled_operations.rb |
| db/schema.rb current | `db/schema.rb:641-642,653` | `stalled_at`, `stalled_reason`, `index_operations_stalled` present from M01-14 migration; version 2026_09_10_000800 matches `create_resource_locks.rb` |
| RecoverStalledOperations scope | `app/commands/recover_stalled_operations.rb:65-70` | Queries by team_id and owner (tenant-scoped); marks stalled via separate method, does not write status directly |

**Assessment:** PASS

## Arbitration Repairs Verified

The Story underwent two arbitrations (DECISIONS.md:170-200):

1. **Spec cleanup (line 170):** Fixed `after(:each)` blocks in serialization and concurrent_reads specs to use proper transaction ordering and delete rescue. **VERIFIED:** Both specs now use `ActiveRecord::Base.transaction` with no rescue.

2. **Schema dump (line 186):** Regenerated db/schema.rb from fully-migrated database to restore M01-14 columns. **VERIFIED:** `stalled_at`, `stalled_reason`, `index_operations_stalled` all present at correct locations.

## Test Evidence

- **Command:** Most recent: `tmp/test-results/runs/17519-1789102977.json`
- **Result:** PASS (exit_status: 0)
- **Tests:** 1118 total, 0 failures, 0 errors
- **M01-15 tests included:** resource_lock_fencing_spec.rb, resource_lock_concurrent_reads_spec.rb, resource_lock_serialization_spec.rb, resource_lock_successor_revalidation_spec.rb, resource_lock_ttl_expiry_spec.rb, fencing_token_spec.rb, resource_lock_ttl_spec.rb, resource_lock_owner_spec.rb

**Assessment:** PASS

## Observability

- Logs at acquisition, renewal, release with scope_key, owner, fencing_token, team_id fields (lines 154-161 of acquire_resource_lock.rb, etc.)
- Orphaned lock release logged with recovery.lock_released_orphaned event (line 211-218 of recover_stalled_operations.rb)
- Fencing token validation errors raised with correlation context

**Assessment:** PASS

## Dependencies

No new gems added. Story uses only:
- PostgreSQL features (ON CONFLICT, RETURNING, xmax)
- Ruby stdlib (SecureRandom, Time)
- Existing Opanel libraries (Identifier, WorkerIdentity, Configuration, Result)

---

## Summary

All 10 acceptance criteria verified and tested. All arbitration repairs applied and confirmed. Tests green (1118 examples, 0 failures). No Critical or High findings. The Story is complete and correct per specification.

COUNTS 0 0 0 0
