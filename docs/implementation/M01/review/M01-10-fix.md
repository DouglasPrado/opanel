# M01-10 — Fix Round Review (2026-09-10)

## Overview
This review evaluates the arbitrated fix round (DECISIONS.md 2026-09-10 blocks at lines 58–71, 74–87) that re-opened M01-10 to address a job class that could not be loaded in production.

## Findings

### 1. Acceptance Criteria
No acceptance criteria are affected by this round. M01-10's original AC1–AC10 remain satisfied. AC11 is properly deferred per arbitration (DEBT, documented in DECISIONS.md 2026-09-10 at 12–22).

### 2. Boundary and Scope
**Observation:** The round declares `lib/rubocop/cop/opanel/unscoped_tenant_query.rb` and `spec/gates/unscoped_tenant_query_spec.rb` under M01-10's boundary at `boundaries.yml:620–621`. The comment explains this is for cop maintenance (tightening to cover models added in M01-11 and M01-12), not for editing the gate spec file itself.

**Verification:** No change to the gate spec file appears in the diff; only the cop file is modified (line 195 adds `Environment Service` to `TENANT_SCOPED`). The comment is accurate and the declaration is necessary.

No files fall outside the declared M01-10 boundary. Scope is correct.

### 3. Architecture Invariants
All architecture invariants are preserved:
- **Cluster-first:** The job remains a Solid Queue worker, now properly typed.
- **Secrets:** The job's logging calls `Rails.logger` with structured fields; no secrets are passed.
- **Desired/Actual State:** No persistent state is modified by this round.
- **Docker isolation:** No new Docker access introduced.
- **MCP path:** Not applicable to job configuration.

### 4. Security
No security regressions:
- The job is properly configured to run with `queue_as Opanel::Queues::SYSTEM`, limiting concurrency to prevent abuse of resources.
- No credentials or sensitive data are exposed by the changes.
- Authorization checks (Team membership required for Node reads) are unchanged.

### 5. Test Strength
**Job class examples:** The new examples in `spec/unit/observe_cluster_nodes_spec.rb:8–23` correctly:
- Load the class explicitly (preventing silent NameError at line 207 RSpec.describe)
- Assert inheritance from `ApplicationJob`
- Test enqueuing via `perform_later` with the `:test` queue adapter

These examples prevent recurrence of the defect: the class is no longer reachable only through a rescue block.

**Spec example type and helpers:** Marked `type: :integration` with `include ActiveJob::TestHelper`, which is the standard pattern for Rails job specs.

**Cop tightening:** `unscoped_tenant_query_spec.rb` was not modified (as documented in boundaries.yml comment). Grep over `app/` found no call sites of `Environment.find(…)` or `Service.find(…)` that would newly violate the cop.

No tests were weakened or removed; new tests added genuine coverage of the class-loading path.

### 6. Gate Status and Documentation
**Format/Lint failures:** Both report four `Layout/SpaceInsideArrayLiteralBrackets` offences in `lib/gates/swarm_lab.rb:101,109,111`, introduced by commit `21a5852`. The report (line 119–123) documents these as pre-existing with evidence: `git checkout -- lib/gates/swarm_lab.rb` + `bundle exec rubocop --force-exclusion lib/gates/swarm_lab.rb` confirms four offences; `/tmp/gate-m0112-b.log` shows the same file passing under M01-12's scope (which does not include this file). The gate shows these are pre-existing, not introduced by this round.

**Tests failures (5 failures in gate_scripts_spec.rb:390,432,440,449,458):** All report `unknown check: pre-commit-executed`. The report (line 110–114) correctly attributes this to ADR-0008 residue in commit `21a5852`, where `pre-commit-executed` was removed from `lib/gates/post_commit.rb:CHECKS` but not from `bin/gate:243` or the spec. The arbitration explicitly refuses this run permission to edit `bin/gate*` or `spec/gates/gate_scripts_spec.rb` (DECISIONS.md:64–70). Both gate failures are accepted debt per arbitration verdict DEBT (line 81).

**Format/lint and tests reported as RED, not passed or waived:** The report correctly states "Os três checks que continuam vermelhos são vermelhos, não dispensados" (line 108–140), with full explanation of pre-existing causes and explicit reference to the arbitration. This is the required treatment per DECISIONS.md:51 ("a red check is reported red, never as passed or waived").

### 7. Boundary Maintenance Documentation
The report section "Boundary Maintenance" (line 164–167) explains the cop tightening: Environment and Service are models added in M01-11 and M01-12, and the cop's own comment prescribes their addition. The addition tightens coverage without breaking existing code.

**Tasks.json state transitions:** M01-10 correctly transitions from `status: "done"` to `status: "review"` and `attempts: 2` to `attempts: 3`. M01-12 remains `status: "done"` at commit `d0822ed` with no reopening (per DECISIONS.md:66 "M01-12 stays done").

### 8. Implementation Correctness
- **Job class:** `class ObserveClusterNodesJob < ApplicationJob` with `queue_as Opanel::Queues::SYSTEM` follows the precedent of `ExampleCheckpointJob` and aligns with `config/queue.yml` worker pool for the system queue (concurrency: 2 threads, 1 process).
- **Spec examples:** Tests the class loads and can be enqueued; prevents regression.
- **Cop**: List expansion is a tightening, not a relaxation.

### 9. Observability and Correlation
No changes to observability paths. The job's logging is inherited from `ApplicationJob#with_correlation_context`, which already includes `correlation_id` in all job logs.

### 10. Dependencies
No new gems or packages introduced.

---

## Conclusion

The round completes all four ordered items from the arbitration (DECISIONS.md:58–71):
1. ✓ Job class corrected to `< ApplicationJob` with proper queue declaration
2. ✓ Spec examples added that load the class and prevent silent failures
3. ✓ Cop tightened to cover Environment and Service (no call sites broken)
4. ✓ Boundaries declared with documented reason

Gate failures (format, lint, tests) are pre-existing and properly documented as debt, with evidence of cause and arbitration ruling. No scope creep, no regression, no weakened tests.

COUNTS 0 0 0 0
