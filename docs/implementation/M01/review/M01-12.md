Based on my review of the Story M01-12 diff, acceptance criteria, architecture, security, and prior arbitration decisions, here are my findings:

## REVIEW FINDINGS FOR M01-12

### Acceptance Criteria Verification

**AC1 (No Docker calls):** SATISFIED. No Docker API calls in Services-related code; only references are to `docker.io` as a registry default. Pattern consistent with image reference validation only.

**AC2 (Unique partial index):** SATISFIED. Migration adds `UNIQUE(environment_id, slug) WHERE deleted_at IS NULL` index at line 1411. Test expectations in `spec/requests/services_spec.rb` cover duplicate and deleted slug reuse.

**AC3 (desired_revision increments):** SATISFIED. Column initialized to 1; `UpdateServiceDesiredState` increments only when reconciler-observable fields change (replicas, image, resources, ports, health_check).

**AC4 (expected_revision with REVISION_CONFLICT):** SATISFIED. Type coercion added at `update_service_desired_state.rb:289` with `Integer(expected_revision)`. Concurrent test with barrier at `spec/integration/service_revision_conflict_spec.rb:25-62` covers lost-update scenario. HTTP status `:conflict` correctly mapped at `services_controller.rb:582`.

**AC7 (Mutable tag resolution):** CORRECTLY DEFERRED. Report line 24 marks as `☐ DEFERRED` per DECISIONS.md SC-21. Migration adds nullable `image_digest` column (line 1369), populated only when user pins `@sha256:` digest, left NULL for mutable tags. No registry read attempted; reconciliation deferred to M01-18 per architecture.

**AC10 (No sensitive fields):** SATISFIED. Service model has no secret/password/token fields. `image_ref` validated by OCI syntax. `audit_sanitizer.rb` allowlists only non-sensitive fields (line 10-13).

**AC11 (Authorization with cross-team negative):** CODE CORRECT, DOCUMENTATION INACCURATE. Commands return `FORBIDDEN` code at `create_service.rb:91`, `update_service_desired_state.rb:283`, `service_runtime_view.rb:1088`. HTTP status mapping at `services_controller.rb:580` correctly maps `FORBIDDEN` → 403. Tests at `spec/requests/services_spec.rb:2813` (in-team VIEWER → `:forbidden`), 2836 (cross-team → `:not_found`), 2902 (stale revision → `:conflict`) all correct. **BUT** report AC11 line 28 states "return CONFLICT if denied" when code emits FORBIDDEN — this is inaccurate documentation.

### Architecture

**Desired State in PostgreSQL:** Service table persists intent; no state machine, only DRAFT initial status per M01-18 reconciliation model.

**MCP path:** Queries and Commands operate through TenantScope; no direct Docker access.

**Audit trail:** `AuditTrail.record` called inside transaction at `create_service.rb:177` and `update_service_desired_state.rb:388`.

**Tenancy scoping:** Service joined through Environment→Project→Team. `tenant_scope.rb` updated to scope Service (line 1196). Cross-team negative tests expect 404 (not disclosure of access denial).

### Security

**Authorization:** Server-side Policy checks before any mutation. FORBIDDEN for insufficient role, NOT_FOUND for non-existent or cross-team resource. Tests confirm VIEWER gets 403 in-team, 404 cross-team.

**Input validation:** `Opanel::ImageRef` validates OCI syntax, rejects malformed references. Ports, replicas, resources all validated at boundary.

**No secrets persisted:** Fields allowlisted in `audit_sanitizer.rb` exclude any secret columns. Image references validated as syntax only.

**Idempotency:** Optimistic concurrency with `expected_revision` prevents lost updates; REVISION_CONFLICT returned without mutation.

### Scope

All touched files declared in `boundaries.yml:696-760`, including:
- Database schema and migrations
- Models, Commands, Queries, Policies
- Image reference validator at `lib/opanel/image_ref.rb`
- Authorization and TenantScope
- Routes and controller
- React frontend
- Audit integration
- Tests (unit, integration, policy, security, frontend)
- `docs/implementation/SPEC_CONFLICTS.md` declared line 760

### Test Coverage

All Story-specific tests pass (100 examples, 0 failures per report line 108-110). Six pre-existing failures in `spec/gates/` reproduced at HEAD per baseline; not attributable to M01-12.

---

## FINDINGS

### Medium

**Documentation inaccuracy in report:** `docs/implementation/M01/reports/M01-12.md` line 28, AC11 evidence states "both Commands call Policy and return CONFLICT if denied" but the actual implementation returns FORBIDDEN for authorization failures. The code is correct per arbitration decision (DECISIONS.md line 50-51); the report description is inaccurate. Audit actions table updated correctly (`spec/unit/audit_actions_spec.rb:3016-3017`).

---

COUNTS 0 0 1 0
