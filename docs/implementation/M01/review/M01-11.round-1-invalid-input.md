# M01-11 — review round 1 (VOID: invalid input, preserved as evidence)

**This round is not the Story's review.** The lead prepared the diff with
`git diff $(bin/story-scope base M01-11)`, which resolved to `41e9bf7` — the commit of
`M01-10`. HEAD at review time was `21a5852` ("perf(gate): take the pre-commit gate out of
the commit path, and bound Docker"), a change committed outside the Story flow. The diff
handed to the reviewer therefore contained that commit's files
(`bin/autopilot`, `docs/decisions/ADR-0008-pre-commit-out-of-the-commit-path.md`,
`docs/implementation/GATE_MAINTENANCE.md`, `lib/gates/post_commit.rb`, `lib/gates/swarm_lab.rb`)
on top of the M01-11 working tree.

The reviewer read that input correctly and reported those files as outside the declared
`M01-11` boundary. It was right about the input and the input was wrong. The finding does not
describe the builder's work, and its counts are not recorded against the Story.

Round 2 was run against `git diff 21a5852`, which is exactly the uncommitted M01-11 change set.
Nothing in the reviewer's response below was altered.

---

## Review: M01-11 — Environment entity with cluster placement

**Reviewer: independent**

**Base commit: 41e9bf7**

### 1. Acceptance Criteria

All 11 criteria declared in the Story. Mapping to test evidence:

| # | Criterion | Test Evidence | Status |
|---|-----------|---------------|--------|
| 1 | Project receives Environments with type and Cluster | `spec/integration/environment_constraints_spec.rb` | PASS |
| 2 | `UNIQUE(projectId, slug) WHERE deletedAt IS NULL` in database | `spec/integration/environment_constraints_spec.rb` + migration CHECK | PASS |
| 3 | Environment references Cluster; Project not hierarchically under Cluster | `spec/integration/environment_constraints_spec.rb`, schema FK only | PASS |
| 4 | Cross-team Cluster access denied without revealing existence | `spec/integration/environment_cross_team_cluster_spec.rb` | PASS |
| 5 | `autoPromoteSecrets` defaults false in PRODUCTION | Migration CHECK constraint + `spec/unit/environment_revision_spec.rb` | PASS |
| 6 | `desiredRevision` and `appliedRevision` exist and increment | `spec/unit/environment_revision_spec.rb` | PASS |
| 7 | Status is derived, no stored boolean | `spec/unit/environment_status_spec.rb` | PASS |
| 8 | PRODUCTION has persistent visual badge | Report marks PENDING; badge component exists but Story scope ambiguous | **UNVERIFIED** |
| 9 | DEGRADED Cluster produces warning/block | Report marks PENDING at M01-18 | **UNVERIFIED** |
| 10 | Mutations generate AuditLog and pass Policy with cross-team negative | `spec/policies/cross_team_mutations_spec.rb` + audit trail | PASS |
| 11 | Archiving Project with active Environment blocked | `spec/unit/project_lifecycle_spec.rb` (AC5 inheritance from M01-07) | PASS |

**Finding**: AC8 status is ambiguous. Report states "UI not in scope of M01-11 backend" but the Story's Scope section (line 23-24 of story file) explicitly requires "Badge persistente de `PRODUCTION` na UI" and "Criação com escolha explícita do Cluster; validação...". The Environments page exists (`app/frontend/pages/Panel/Environments.tsx`, added in diff). Whether AC8 is satisfied depends on whether the badge is actually rendered on that page. The diff shows the component updated to include CUSTOM and HOMOLOGATION types, but visual persistence on the UI page itself is not evident in the code review.

---

### 2. Boundary Compliance

**HIGH FINDING: Out-of-boundary changes not justified in Story report.**

The declared boundary for M01-11 (lines 642-698 in boundaries.yml as updated in this diff) does NOT include:

1. **`bin/autopilot`** — Modified at lines 1093-1145 in diff. Adds watchdog for hung sessions (20-minute no-file-write detection). Not in declared boundary; not mentioned or justified in Story report.

2. **`docs/decisions/ADR-0008-pre-commit-out-of-the-commit-path.md`** — New file added at lines 1502-1581 in diff. Records architectural decision to remove pre-commit gate from commit path. This is a tier-0 decision per AGENT_RULES (gate modifications require ADR). Not in declared boundary; ADR header says "accepted on the owner's explicit instruction" but this is not a Story authorization mechanism.

3. **`docs/implementation/GATE_MAINTENANCE.md`** — Modified at lines 1582-1626 in diff. Adds debt tracking GM-05 and GM-06. Not in declared boundary; not mentioned in Story report.

**Rule violated**: CLAUDE.md §Scope Discipline and AGENT_RULES §Scope Discipline: "Changes outside the declared boundary are a review finding even when the code is correct." The Story's report makes no mention of these changes, their justification, or why the boundary was expanded. A boundary written after implementation describes what happened rather than preventing scope creep.

---

### 3. Architecture Invariants

**No findings.** Verified:

- **Desired State in PostgreSQL**: `environments` table with schema constraints, proper FK relationships. OK
- **Tenant scoping**: Denormalized `team_id` in environments table, `TenantScope` registration, `accessible_to` scope. OK
- **Authorization boundary**: `EnvironmentPolicy` reached by Commands before any mutation. AF-07 entry added to fitness.yml. OK
- **No Docker boundary violated**: No executor changes. OK
- **Soft delete pattern**: `deleted_at` field with partial unique index. OK
- **Audit path**: Both Commands call `AuditTrail.record()` inside transaction; Actions registered. OK

---

### 4. Security

**No findings.**

- **Tenancy isolation**: Cross-team Cluster lookup tested; AC4 verified. TenantScope used for all queries. OK
- **Authorization server-side**: EnvironmentPolicy enforces ADMIN/DEVELOPER roles. Unsaved resource pattern (temporary Environment instance) used for authorization gate. OK
- **Secrets redaction**: No Environment fields are sensitive. Audit allowlist excludes secrets correctly. OK
- **SSRF/external access**: No URL fetching in any new code. OK

---

### 5. Test Adequacy

**No findings on test weakening.** All acceptance criteria have test mappings. Unit and integration specs for status machine, revision tracking, constraint enforcement, cross-team leak path exist. Policy matrix tested. Negative cases planted (duplicate slug, cross-team access).

**Minor concern**: Report states "Quality gates: PENDING (AF-07 checker needs investigation for unsaved resource authorization)" — suggests full gate run not completed and potential fitness function issue with unsaved resource detection. However, this is conditional on gate completion, not a test failure.

---

### 6. Dependencies

No new gem dependencies. Uses existing patterns (ULID, soft delete, keyset pagination). OK

---

### 7. Authorization and Audit

**No findings.**

- `CreateEnvironment`: Policy gated at line 46, AuditTrail recorded at line 178. OK
- `UpdateEnvironment`: Policy gated at line 256, AuditTrail recorded at line 299. OK
- `ArchiveProject`: Now checks `project.environments.kept.exists?` (line 78 in archive_project.rb) to block when active Environments exist. OK
- Actions registered in `AuditLog::ACTIONS` and allowlisted in `AuditSanitizer`. OK

---

## Summary

**Critical findings**: 1 (scope creep outside boundary)
**High findings**: 1 (AC8 verification gap)
**Medium findings**: 0
**Low findings**: 0

### Blocking Issues

1. **HIGH: Out-of-boundary changes (bin/autopilot, ADR-0008, GATE_MAINTENANCE)** — These modifications are not declared in the Story's boundary and are not justified in the report. Per AGENT_RULES, changes outside boundary are findings even when correct. The Story must either: (a) declare these files in its boundary with justification, or (b) revert them to a separate commit/Story.

2. **HIGH: AC8 verification incomplete** — Acceptance Criterion 8 (PRODUCTION has persistent visual badge) is marked PENDING in the report. The Story declares this in its Scope. The component exists and was updated, but whether it is actually rendered persistently on the Environments page is not evident from code review. Clarification needed: does the diff show the badge is persistent on the UI page, or is this truly deferred to a later Story?

Both High findings must be resolved before DONE.

---

```
COUNTS 0 2 0 0
```
