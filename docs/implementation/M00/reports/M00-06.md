# Story Report — M00-06 Reproducible local development environment

## Resultado

- Story: `M00-06` — Reproducible local development environment
- Status: `completed`
- Commit: `b8a5f82`
- Milestone: `M00`

## Arquivos alterados

| Arquivo | Mudança |
|---|---|
| `bin/setup` | Checks, installs, prepares, seeds, starts. Idempotent; guardrail first. |
| `bin/dev` | Supervises web, worker and Vite in one foreground command. |
| `README.md` | Prerequisites, the single command, reset, teardown. |
| `db/seeds.rb` | Development seeds with no real data. |
| `app/views/layouts/application.html.erb` | `vite_react_refresh_tag` before the client tag. |
| `config/vite.json` | Dev host pinned to `127.0.0.1`. |
| `config/initializers/content_security_policy.rb` | `unsafe-inline` for script, development only. |
| `app/controllers/health_controller.rb` | Queue check rescues `ActiveRecord::ActiveRecordError`. |
| `spec/integration/development_environment_spec.rb` | The harness, proven. |

## Schema / API / Events

`none`.

## Decisões locais

- **The workspace guardrail runs before anything is installed or connected.** If
  this machine holds a production credential, nothing else should run on it — and
  checking after `bundle install` has already run means checking after the machine
  has already been used.
- **`bin/dev` is written here rather than delegated to foreman**, for the one
  behaviour that matters: when a process dies it says *which* and stops the
  others. A supervisor that leaves the web server up while the worker is silently
  dead is how an afternoon disappears. An occupied port names the process holding
  it, because "address already in use" does not.
- **The queue health check rescues `ActiveRecord::ActiveRecordError`, not
  `StandardError`.** Reporting a `NoMethodError` in our own code as "the queue is
  unavailable" sends an operator to look at the wrong system. The custom cop from
  M00-09 caught this; the fix was to narrow the rescue rather than to weaken the
  cop.
- **Development needs `script-src 'unsafe-inline'`** for the React Refresh
  preamble. Scoped to development, with a spec asserting test and production keep
  `script-src 'self'` — which is the directive an XSS actually needs.

## Testes executados

| Comando | Resultado |
|---|---|
| `bin/setup --skip-server` (clean clone at `/tmp/opanel-clone`) | exit 0 |
| `bin/setup --skip-server` (second run) | exit 0, same state |
| `bin/dev` + Playwright against `:3021` | Inertia page rendered, React mounted |
| `bundle exec rspec spec/integration/development_environment_spec.rb` | 21 examples, 0 failures, exit `0` — re-run at fix attempt 03, see below |
| `bin/setup` with `PATH` stripped of Node | exit 1, names the tool and the remedy |

## Quality Gates

Local Quality Gate green. `bin/lint`, `bin/format --check`, `bin/typecheck`,
`bin/fitness` all pass.

## Acceptance Criteria

| # | Critério | Evidência |
|---|---|---|
| 1 | Clone limpo serve página Inertia | cloned to `/tmp/opanel-clone`, `bin/setup --skip-server`, `bin/dev`, driven with Playwright — page rendered and React mounted |
| 2 | `bin/setup` idempotente | two consecutive runs, both exit 0; `spec/integration/development_environment_spec.rb`: `"are idempotent: running them twice changes nothing the second time"` |
| 3 | `bin/dev` sobe os três; queda é visível | `bin/dev`, asserted structurally in `spec/integration/development_environment_spec.rb`: `"starts the web server, the worker and Vite"` and `"stops the others when one exits, so a dead process is not hidden"`. The supervisor's behaviour under a real child crash is not exercised by a running process — recorded as a gap in Pendências |
| 4 | Seeds aplicam, sem dado real | `bin/rails db:seed` exit 0; seeds contain only literal placeholders |
| 5 | README documenta pré-requisitos, comando, reset e destruição | `README.md` |
| 6 | Falha acionável com pré-requisito ausente | negative test using `RbConfig::CONFIG["bindir"]` + `/usr/bin:/bin` |
| 7 | Nenhuma credencial de produção; setup falha se encontrar | `bin/workspace-guardrail` runs first in `bin/setup` |

## Dependências novas

`none`.

## Pendências / conflitos

The clean-clone verification is the only thing that found the two development
defects (missing `vite_react_refresh_tag`, IPv6-only Vite). Neither the HMR check
of M00-04 nor the E2E of M00-08 could have: the first only proved the dev server
re-transformed a module, and the second runs against a built bundle. Worth
repeating before any Milestone that changes the asset pipeline.

`bin/dev`'s supervision is asserted by reading the script, not by killing a child
of a running supervisor. The two properties that matter — that a dead child stops
the others, and that its name reaches the output — are both visible in the source
and both asserted; what is not covered is the process actually doing it. Carried
as a known gap rather than described as a runtime test.

**Corrected at fix attempt 03, answering `MILESTONE_REVIEW_03.md` F02.** The
"Arquivos alterados" and "Testes executados" tables named
`spec/integration/setup_spec.rb` and `spec/integration/dev_supervisor_spec.rb` and
recorded an rspec run over both as green. Neither file has ever existed on any
branch — `git log --all` for both paths is empty — so the run could not have
happened: rspec against a missing path errors, it does not pass. The spec that
exists, and that covers those properties, is
`spec/integration/development_environment_spec.rb`, added at `b8a5f82` with the
rest of the Story. `FIX_REPORT_02.md` found one of the two names while fixing
M00-R07 and corrected only the Acceptance Criteria row the gate reads, which left
the same false claim standing in two other tables of this file. The rows above are
now the command actually run and its count.

**Also corrected here at fix attempt 03**, from the same round's repair of the CI
pipeline: `bin/setup` probed PostgreSQL with a bare `pg_isready`, which asks the
local Unix socket. That is right on a laptop and wrong wherever the database is a
TCP service, and it failed the `setup` CI job against a PostgreSQL that
`bin/rails db:test:prepare` had connected to seconds earlier. It now reads
`OPANEL_DATABASE_HOST` and `OPANEL_DATABASE_PORT`, the same declarations
`config/database.yml` reads, with the same defaults.
